# Dzien 1 (2026-07-24) - Podstawy skladni Robot Framework

## 1. Temat dnia
Podstawy skladni Robot Framework - struktura plikow .robot, sekcje, separator kolumn, komentarze.

## 2. Kluczowe zasady i najlepsze praktyki
- Plik .robot dzieli sie na sekcje: *** Settings ***, *** Variables ***, *** Test Cases ***, *** Keywords *** - nazwy sekcji musza byc dokladnie takie.
- Kolumny (argumenty slow kluczowych) rozdziela sie co najmniej dwoma spacjami albo znakiem tabulacji - jedna spacja nie wystarczy i RF potraktuje tekst jako jeden argument.
- Nazwy testow i slow kluczowych pisz w stylu czytelnym dla czlowieka (Title Case, pelne zdania), np. "Zaloguj uzytkownika z poprawnymi danymi" - to jednoczesnie dokumentacja testu.
- Uzywaj "..." (trzy kropki) do kontynuacji dlugiej linii - poprawia czytelnosc w repo i podczas code review.
- Komentarze zaczynaj od # na poczatku linii lub jako osobna kolumna - nie mieszaj kodu i komentarza w tej samej "komorce".

## 3. Przykladowy kod
```robotframework
*** Settings ***
Library           BuiltIn
Documentation     Przykladowy test pokazujacy podstawowa skladnie RF.

*** Variables ***
${URL}            https://example.com
${BROWSER}        chrome

*** Test Cases ***
Sprawdzenie podstawowego dodawania
    [Documentation]    Test weryfikuje dzialanie prostego slowa kluczowego.
    [Tags]    smoke    podstawy
    ${wynik}=    Evaluate    2 + 2
    Should Be Equal As Integers    ${wynik}    4

Test z dluga linia i kontynuacja
    Log Many
    ...    Pierwsza linia logu
    ...    Druga linia logu
    ...    Trzecia linia logu
```

## 4. Najczestsze bledy
- Rozdzielanie argumentow pojedyncza spacja zamiast dwiema - RF zlepia argumenty w jeden string.
- Literowki w nazwach sekcji (np. *** Test Case *** zamiast *** Test Cases ***) - sekcja zostaje zignorowana bez wyraznego bledu.
- Mieszanie tabulacji i spacji w tym samym pliku, co powoduje trudne do zdiagnozowania bledy parsowania.
- Brak [Documentation] i [Tags] - testy staja sie nieczytelne przy wiekszej skali projektu.

## 5. Ulepszenia z AI
- Poproś Claude/Copilot o wygenerowanie szkieletu pliku .robot na podstawie opisu scenariusza w jezyku naturalnym ("napisz test logowania z niepoprawnym haslem").
- Wklej istniejacy plik .robot i poproś AI o wykrycie bledow skladniowych (np. zle odstepy, brakujace sekcje) przed uruchomieniem testu.
- Uzyj AI do konwersji nieformalnych krokow testowych (np. z Jira/user story) na gotowa strukture Test Case + Keywords.
- Poproś o wygenerowanie dokumentacji [Documentation] na podstawie krokow testu, jesli zostala pominieta.

## Zadanie na dzis
Napisz plik .robot z sekcjami Settings, Variables i Test Cases, zawierajacy jeden test, ktory uzywa co najmniej dwoch wbudowanych slow kluczowych (Log, Should Be Equal, Evaluate itp.) oraz poprawnie sformatowana kontynuacje linii (...).

================================================================================

# Dzien 2 (2026-07-25) - Slowa kluczowe wbudowane (BuiltIn)

## 1. Temat dnia
Slowa kluczowe wbudowane z biblioteki BuiltIn - najczesciej uzywane keywordy dostepne w kazdym pliku .robot bez importu.

## 2. Kluczowe zasady i najlepsze praktyki
- Biblioteka BuiltIn jest importowana domyslnie - nie trzeba jej dodawac w sekcji Settings, zawiera m.in. Log, Should Be Equal, Set Variable, Run Keyword If, Evaluate.
- Do porownan uzywaj wlasciwego typu asercji: Should Be Equal (stringi), Should Be Equal As Integers/Numbers (liczby) - porownanie stringa z liczba bez konwersji czesto konczy sie falszywym PASS lub myllacym FAIL.
- Unikaj nadmiernego zagniezdzania Run Keyword If - powyzej 2 warunkow czytelniej jest napisac wlasny Custom Keyword lub uzyc IF/ELSE (nowa skladnia RF 5+).
- Set Variable, Set Test Variable, Set Suite Variable i Set Global Variable maja rozny zasieg - dobierz najwezszy mozliwy, zeby nie "wyciekaly" zmienne miedzy testami.
- Uzywaj Should Contain, Should Match, Should Start With zamiast recznego parsowania stringow - kod jest krotszy i czytelniejszy w log.html.

## 3. Przykladowy kod
```robotframework
*** Test Cases ***
Weryfikacja slow kluczowych BuiltIn
    [Documentation]    Przyklad uzycia popularnych keywordow z biblioteki BuiltIn.
    ${suma}=    Evaluate    10 + 5
    Should Be Equal As Integers    ${suma}    15

    ${status}=    Set Variable    aktywny
    IF    '${status}' == 'aktywny'
        Log    Uzytkownik jest aktywny    console=True
    ELSE
        Fail    Nieoczekiwany status uzytkownika
    END

    ${tekst}=    Set Variable    Robot Framework to super narzedzie
    Should Contain    ${tekst}    Framework
    Should Not Be Empty    ${tekst}

    Run Keyword If    ${suma} > 10    Log    Suma wieksza niz 10
    ...    ELSE    Log    Suma mniejsza lub rowna 10
```

## 4. Najczestsze bledy
- Uzywanie Should Be Equal do porownania liczb bez konwersji typu (np. porownanie ${x} z "5" jako string zamiast liczby).
- Nadmiar zagniezdzonych Run Keyword If zamiast czytelnego IF/ELSE lub osobnego keywordu.
- Nadpisywanie zmiennych globalnych (Set Global Variable) tam, gdzie wystarczy Set Test Variable - powoduje trudne do namierzenia zaleznosci miedzy testami.
- Ignorowanie wartosci zwracanej przez Evaluate/Set Variable i poleganie na efektach ubocznych zamiast jawnych asercji.

## 5. Ulepszenia z AI
- Poproś Claude/Copilot o liste najbardziej przydatnych keywordow BuiltIn do konkretnego scenariusza (np. "jakich slow kluczowych uzyc do walidacji listy stringow").
- Wklej fragment testu z zagniezdzonymi Run Keyword If i poproś AI o refaktoryzacje do czytelnej skladni IF/ELSE.
- Uzyj AI do wygenerowania zestawu asercji (Should Be Equal, Should Contain, Should Match Regexp) na podstawie opisu oczekiwanego zachowania w jezyku naturalnym.
- Poproś o wyjasnienie roznicy miedzy Set Test/Suite/Global Variable w kontekscie konkretnego projektu, gdy nie jest jasne, ktorego uzyc.

## Zadanie na dzis
Napisz test, ktory pobiera wartosc liczbowa przez Evaluate, sprawdza ja warunkiem IF/ELSE, a nastepnie uzywa co najmniej dwoch roznych asercji Should * (np. Should Contain i Should Be Equal As Integers) na wynikach posrednich.

================================================================================

# Dzien 3 (2026-07-26) - Zmienne i ich zasiegi

## 1. Temat dnia
Zmienne w Robot Framework - typy zmiennych (${scalar}, @{list}, &{dict}, %{env}) oraz ich zasiegi (scopes): lokalny, testowy, suite'owy i globalny.

## 2. Kluczowe zasady i najlepsze praktyki
- RF ma cztery typy zmiennych: ${scalar} (pojedyncza wartosc), @{list} (lista), &{dict} (slownik), %{env} (zmienna srodowiskowa) - dobierz typ zgodnie z rzeczywista struktura danych, nie tylko ${} do wszystkiego.
- Zasieg domyslny zmiennej ustawionej przez "${x}=    Keyword" wewnatrz test case'u to zasieg lokalny (tylko biezacy test) - jesli potrzebujesz przekazac wartosc dalej, uzyj Set Test/Suite/Global Variable.
- Hierarchia zasiegow od najwezszego do najszerszego: lokalny (Keyword/Test) -> Set Test Variable -> Set Suite Variable -> Set Global Variable - zawsze wybieraj najwezszy mozliwy, by uniknac wyciekania stanu miedzy testami.
- Zmienne zdefiniowane w sekcji *** Variables *** sa dostepne w calym suite od momentu wczytania pliku - nadpisuj je w razie potrzeby przez wiersz polecen (-v NAZWA:wartosc) zamiast edytowac plik.
- Do zmiennych srodowiskowych i konfiguracyjnych uzywaj %{ZMIENNA_SRODOWISKOWA} lub Get Environment Variable z wartoscia domyslna, zamiast zakladac, ze zmienna zawsze istnieje.

## 3. Przykladowy kod
```robotframework
*** Settings ***
Library           Collections
Library           OperatingSystem

*** Variables ***
${BASE_URL}       https://example.com
@{UZYTKOWNICY}    admin    tester    gosc
&{DANE_LOGOWANIA}    login=admin    haslo=Sekret123

*** Test Cases ***
Zmienne scalar, list i dict
    [Documentation]    Prezentacja roznych typow zmiennych i ich zasiegow.
    Log    Adres bazowy: ${BASE_URL}

    FOR    ${uzytkownik}    IN    @{UZYTKOWNICY}
        Log    Przetwarzam uzytkownika: ${uzytkownik}
    END

    Log    Login: ${DANE_LOGOWANIA}[login]

    ${licznik}=    Set Variable    0
    Set Test Variable    ${licznik}    1
    Set Suite Variable    ${OSTATNI_TEST}    Zmienne scalar, list i dict

Odczyt zmiennej srodowiskowej z wartoscia domyslna
    ${env}=    Get Environment Variable    RF_ENV    default
    Should Not Be Empty    ${env}
    Log    Srodowisko testowe: ${env}
```

## 4. Najczestsze bledy
- Ustawianie zmiennej lokalnie (${x}=    Keyword) i oczekiwanie, ze bedzie widoczna w kolejnym tescie - bez Set Test/Suite Variable znika po zakonczeniu testu.
- Nadmierne uzycie Set Global Variable "na wszelki wypadek" - powoduje trudne do wytropienia zaleznosci i testy, ktore dzialaja tylko w okreslonej kolejnosci.
- Mieszanie typow: dostep do listy jak do zmiennej skalarnej (${UZYTKOWNICY} zamiast @{UZYTKOWNICY} przy iteracji) - RF nie zawsze zglosi to jako blad, ale zachowanie bedzie nieoczekiwane.
- Brak wartosci domyslnej przy odczycie zmiennych srodowiskowych/konfiguracyjnych - test wywala sie z niejasnym bledem na innym srodowisku (np. lokalnie vs CI).

## 5. Ulepszenia z AI
- Poproś Claude/Copilot o przeanalizowanie istniejacego suite'u i wskazanie, gdzie zmienne maja zbyt szeroki zasieg (np. nadmiar Set Global Variable) wraz z propozycja zawezenia.
- Wklej fragment testu z powtarzajacymi sie literalami (URL-e, dane logowania) i poproś AI o wyodrebnienie ich do sekcji *** Variables *** lub pliku resource.
- Uzyj AI do wygenerowania przykladowych danych testowych jako @{list} lub &{dict} na podstawie opisu struktury (np. "wygeneruj slownik z 5 polami uzytkownika").
- Poproś o wyjasnienie, dlaczego konkretna zmienna "nie dziala" w kolejnym tescie/keywordzie - AI szybko wskaze brakujacy Set Test/Suite/Global Variable.

## Zadanie na dzis
Napisz suite z co najmniej dwoma testami: w pierwszym ustaw zmienna przez Set Suite Variable, a w drugim odczytaj i zweryfikuj jej wartosc. Dodatkowo zdefiniuj zmienna typu @{list} i &{dict} w sekcji *** Variables *** i uzyj obu w petli FOR oraz przy dostepie do konkretnego klucza slownika.

================================================================================

# Dzien 4 (2026-07-27) - Test/Suite/Task setup i teardown

## 1. Temat dnia
Setup i teardown na poziomie testu, suite'u i taska (RPA) - jak przygotowywac srodowisko przed testami i sprzatac po nich niezaleznie od wyniku.

## 2. Kluczowe zasady i najlepsze praktyki
- Suite Setup/Suite Teardown (sekcja *** Settings ***) uruchamiaja sie raz na caly plik/suite - idealne do kosztownych operacji, np. logowanie, start serwera, otwarcie przegladarki.
- Test Setup/Test Teardown w *** Settings *** ustawiaja domyslne zachowanie dla WSZYSTKICH testow w pliku; [Setup]/[Teardown] zdefiniowany bezposrednio przy tescie NADPISUJE domyslny, a nie dodaje sie do niego.
- Teardown wykonuje sie zawsze - rowniez wtedy, gdy test/suite zakonczyl sie bledem, timeoutem lub Fatal Error - dlatego to wlasciwe miejsce na zamykanie zasobow (przegladarka, polaczenia, pliki tymczasowe).
- Do laczenia kilku slow kluczowych w jednym [Setup]/[Teardown] uzywaj Run Keywords ... AND ..., a przy wiekszej liczbie krokow lepiej wydzielic osobny Custom Keyword - jest czytelniej w log.html.
- W teardownie stosuj Run Keyword If Test Failed (zrzut ekranu, dodatkowe logi tylko przy porazce) oraz Run Keyword And Ignore Error dla krokow sprzatajacych, ktore nie powinny przerywac kolejnych krokow czyszczenia.
- W RPA (Robot Framework jako framework do automatyzacji procesow) analogiczne role pelnia Task Setup/Task Teardown - dzialaja identycznie jak Test Setup/Teardown, ale w kontekscie *** Tasks ***.

## 3. Przykladowy kod
```robotframework
*** Settings ***
Library           SeleniumLibrary
Suite Setup       Otworz przegladarke i zaloguj
Suite Teardown    Zamknij wszystkie przegladarki
Test Setup        Przejdz na strone glowna
Test Teardown     Wykonaj sprzatanie po tescie

*** Test Cases ***
Test z domyslnym teardown
    [Documentation]    Korzysta z Test Setup/Teardown zdefiniowanych w Settings.
    Page Should Contain    Witaj

Test z wlasnym teardown nadpisujacym domyslny
    [Teardown]    Run Keywords    Zrzut ekranu przy bledzie    AND    Wyloguj uzytkownika
    Fail    Celowy blad do demonstracji dzialania teardown

*** Keywords ***
Otworz przegladarke i zaloguj
    Open Browser    https://example.com    chrome
    Input Text    id=login    admin
    Input Text    id=haslo    Sekret123
    Click Button    Zaloguj

Zamknij wszystkie przegladarki
    Close All Browsers

Przejdz na strone glowna
    Go To    https://example.com/dashboard

Wykonaj sprzatanie po tescie
    Run Keyword If Test Failed    Zrzut ekranu przy bledzie
    Run Keyword And Ignore Error    Log    Test zakonczony, sprzatanie wykonane

Zrzut ekranu przy bledzie
    Capture Page Screenshot

Wyloguj uzytkownika
    Click Button    Wyloguj
```

## 4. Najczestsze bledy
- Zalozenie, ze [Teardown] przy tescie "dolacza sie" do Test Teardown z Settings - w rzeczywistosci go zastepuje, przez co domyslne sprzatanie po cichu przestaje dzialac dla tego testu.
- Wykonywanie zrzutow ekranu/dodatkowych logow w kazdym teardownie bez warunku Run Keyword If Test Failed - logi robia sie ogromne i trudne do przegladania.
- Niestabilny teardown, ktory sam rzuca wyjatkiem (np. probuje zamknac przegladarke, ktora nigdy sie nie otworzyla) - warto zabezpieczyc krytyczne kroki sprzatania przez Run Keyword And Ignore Error.
- Umieszczanie w Suite Setup logiki, ktora powinna byc w Test Setup (np. logowanie tego samego uzytkownika przed kazdym testem) - powoduje, ze testy dzieda sie od wspolnego, mutowalnego stanu i nie sa od siebie niezalezne.

## 5. Ulepszenia z AI
- Poproś Claude/Copilot o przeanalizowanie suite'u pod katem brakujacych teardownow - np. gdzie Open Browser/Connect nie ma odpowiadajacego Close/Disconnect.
- Wklej test konczacy sie bledem w teardownie i poproś AI o dodanie Run Keyword And Ignore Error tam, gdzie to bezpieczne, bez ukrywania prawdziwych bledow testu.
- Uzyj AI do wygenerowania wspolnego Custom Keyword laczacego kilka krokow sprzatania (np. wylogowanie + zamkniecie przegladarki + usuniecie plikow tymczasowych) na podstawie opisu w jezyku naturalnym.
- Poproś AI o wyjasnienie, dlaczego dany test "traci" domyslny teardown - czesto wystarczy wskazac, ze [Teardown] przy tescie nadpisuje Test Teardown z Settings.

## Zadanie na dzis
Napisz suite z Suite Setup/Suite Teardown (np. symulowane otwarcie/zamkniecie "polaczenia" przez Log) oraz Test Setup/Test Teardown w Settings. Dodaj dwa testy: pierwszy niech korzysta z domyslnego teardown, a drugi niech ma wlasny [Teardown] uzywajacy Run Keyword If Test Failed do warunkowego logowania dodatkowych informacji.

================================================================================

# Dzien 5 (2026-07-28) - Biblioteki standardowe (Collections, String, OperatingSystem, DateTime)

## 1. Temat dnia
Wbudowane biblioteki standardowe Robot Framework inne niz BuiltIn: Collections (listy/slowniki), String (operacje na tekscie), OperatingSystem (pliki i system) oraz DateTime (data i czas).

## 2. Kluczowe zasady i najlepsze praktyki
- Kazda z tych bibliotek wymaga jawnego importu w sekcji *** Settings *** (Library Collections / String / OperatingSystem / DateTime) - w przeciwienstwie do BuiltIn nie sa dostepne domyslnie.
- Collections udostepnia dedykowane asercje dla list/slownikow (Lists Should Be Equal, Dictionary Should Contain Key) - sa czytelniejsze i daja lepsze komunikaty bledow niz porownywanie recznie przez Should Be Equal.
- String sluzy do manipulacji tekstem (Split String, Strip String, Get Substring, Convert To Upper Case) - unikaj laczenia jej z zewnetrznymi skryptami tam, gdzie wystarczy gotowy keyword.
- OperatingSystem daje dostep do plikow/katalogow/zmiennych srodowiskowych (Create File, Remove File, Directory Should Exist) - przydatne przy testach RPA i przygotowywaniu danych wejsciowych/wyjsciowych.
- DateTime pozwala liczyc i formatowac daty (Get Current Date, Subtract Time From Date, Convert Date) - zawsze podawaj jawny format i strefe czasowa, zeby testy byly powtarzalne niezaleznie od maszyny CI.
- Wybieraj biblioteke adekwatnie do typu danych: Collections dla struktur (list/dict), String dla surowego tekstu - mieszanie ich tam, gdzie jedna wystarczy, zwieksza zlozonosc testu.

## 3. Przykladowy kod
```robotframework
*** Settings ***
Library           Collections
Library           String
Library           OperatingSystem
Library           DateTime

*** Test Cases ***
Operacje na liscie i slowniku
    [Documentation]    Przyklad uzycia biblioteki Collections.
    @{owoce}=    Create List    jablko    gruszka    banan
    Append To List    ${owoce}    pomarancza
    Lists Should Be Equal    ${owoce}    ${{['jablko', 'gruszka', 'banan', 'pomarancza']}}

    &{dane}=    Create Dictionary    imie=Jan    wiek=30
    Dictionary Should Contain Key    ${dane}    imie
    Set To Dictionary    ${dane}    miasto=Warszawa

Operacje tekstowe i plikowe
    [Documentation]    Przyklad uzycia bibliotek String i OperatingSystem.
    ${tekst}=    Set Variable    Robot Framework, wersja 7
    ${czesci}=    Split String    ${tekst}    ,
    ${wielkie}=    Convert To Upper Case    ${tekst}
    Should Contain    ${wielkie}    ROBOT

    Create File    ${OUTPUT_DIR}/raport.txt    Wynik testu: OK
    File Should Exist    ${OUTPUT_DIR}/raport.txt
    ${zawartosc}=    Get File    ${OUTPUT_DIR}/raport.txt
    Should Contain    ${zawartosc}    OK

Operacje na dacie i czasie
    [Documentation]    Przyklad uzycia biblioteki DateTime.
    ${dzis}=    Get Current Date    result_format=%Y-%m-%d
    ${za_tydzien}=    Add Time To Date    ${dzis}    7 days    result_format=%Y-%m-%d
    Log    Dzisiaj: ${dzis}, za tydzien: ${za_tydzien}
```

## 4. Najczestsze bledy
- Uzywanie keywordow z Collections/String/DateTime bez importu odpowiedniej biblioteki - RF zglosi blad "No keyword with name X found", co bywa mylone z literowka w nazwie.
- Porownywanie list/slownikow przez Should Be Equal zamiast Lists Should Be Equal/Dictionaries Should Be Equal - kolejnosc elementow lub typ danych powoduje falszywe FAIL trudne do zdiagnozowania.
- Zakladanie stalego formatu daty zwracanego przez Get Current Date bez podania result_format - test dziala inaczej lokalnie i na serwerze CI w innej strefie czasowej.
- Tworzenie plikow testowych bezposrednio w katalogu projektu zamiast w ${OUTPUT_DIR} - zasmieca repozytorium i utrudnia sprzatanie po testach.

## 5. Ulepszenia z AI
- Poproś Claude/Copilot o wskazanie, ktora biblioteka standardowa (Collections/String/OperatingSystem/DateTime) pasuje do danego zadania, gdy nie jest oczywiste, gdzie szukac keywordu.
- Wklej fragment testu porownujacego listy/slowniki przez Should Be Equal i poproś AI o refaktoryzacje do dedykowanych keywordow z Collections.
- Uzyj AI do wygenerowania przykladowych danych testowych (listy, slowniki, teksty, daty) na podstawie opisu struktury w jezyku naturalnym.
- Poproś AI o wyjasnienie komunikatu bledu "No keyword with name X found" - czesto szybko wskaze brakujacy import biblioteki.

## Zadanie na dzis
Napisz test, ktory: utworzy liste i slownik przez Collections i zweryfikuje ich zawartosc dedykowanymi keywordami, podzieli tekst na czesci przez Split String z biblioteki String, zapisze wynik do pliku w ${OUTPUT_DIR} przez OperatingSystem, a na koniec obliczy date za 7 dni od dzisiaj przez DateTime.

================================================================================


================================================================================
# DZIEN 6: TESTY WEBOWE - SELENIUMLIBRARY / BROWSER LIBRARY (PLAYWRIGHT)
Data: 2026-07-29 (sroda)
================================================================================

## 1. Temat dnia
Automatyzacja testow webowych w Robot Framework - klasyczna **SeleniumLibrary**
oraz nowoczesna **Browser Library** oparta na Playwright. Skupiamy sie glownie
na Browser Library jako aktualnie rekomendowanym wyborze dla nowych projektow.

## 2. Kluczowe zasady i najlepsze praktyki
- Wybieraj **Browser Library (Playwright)** do nowych projektow - dziala szybciej,
  ma wbudowane auto-waity (nie trzeba recznie robic Wait Until Element Is Visible
  tak czesto jak w Selenium) i lepiej obsluguje nowoczesne SPA.
- Uzywaj **selektorow tekstowych/CSS/testid** zamiast kruchych XPath opartych na
  strukturze DOM - `"css=[data-testid=login-button]"` przetrwa refaktoryzacje UI.
- Zawsze otwieraj przegladarke w `Suite Setup` i zamykaj w `Suite Teardown`
  (`New Browser` / `Close Browser`), a nowa strone (`New Page`) per test, jesli
  testy maja byc od siebie niezalezne.
- Stosuj **Page Object Pattern** - trzymaj lokalizatory i akcje na danej stronie
  w osobnym Resource file, testy nie powinny znac szczegolow selektorow.
- Korzystaj z wbudowanych auto-waitow Browser Library zamiast `Sleep` -
  `Click`, `Fill Text` same czekaja az element bedzie interaktywny.

## 3. Przyklad kodu (.robot)
```robotframework
*** Settings ***
Library    Browser
Suite Setup       New Browser    chromium    headless=true
Suite Teardown    Close Browser
Test Setup        New Page    https://example.com/login

*** Variables ***
${LOGIN_INPUT}      css=#username
${PASSWORD_INPUT}    css=#password
${LOGIN_BUTTON}     css=[data-testid=login-button]
${WELCOME_TEXT}     css=.welcome-message

*** Test Cases ***
Poprawne logowanie uzytkownika
    [Documentation]    Sprawdza logowanie z prawidlowymi danymi.
    [Tags]    web    smoke
    Fill Text    ${LOGIN_INPUT}    jan.kowalski
    Fill Text    ${PASSWORD_INPUT}    TajneHaslo123
    Click    ${LOGIN_BUTTON}
    Get Text    ${WELCOME_TEXT}    contains    Witaj, Jan

Blokada logowania przy pustym hasle
    [Documentation]    Weryfikuje komunikat bledu przy braku hasla.
    [Tags]    web    negative
    Fill Text    ${LOGIN_INPUT}    jan.kowalski
    Click    ${LOGIN_BUTTON}
    Get Text    css=.error-message    ==    Haslo jest wymagane
```

Rownowaznik w SeleniumLibrary (dla porownania):
```robotframework
*** Settings ***
Library    SeleniumLibrary

*** Test Cases ***
Logowanie - SeleniumLibrary
    Open Browser    https://example.com/login    chrome
    Input Text    id=username    jan.kowalski
    Input Password    id=password    TajneHaslo123
    Click Button    css=[data-testid=login-button]
    Wait Until Element Is Visible    css=.welcome-message    timeout=5s
    Element Should Contain    css=.welcome-message    Witaj, Jan
    [Teardown]    Close Browser
```

## 4. Najczestsze bledy
- Uzywanie `Sleep` zamiast auto-waitow lub `Wait Until ...` - testy sa wolne
  i niestabilne, bo czas oczekiwania jest zgadywany na sztywno.
- Twarde XPath typu `//div[3]/div[2]/span` - lamie sie przy kazdej zmianie
  ukladu strony; lepiej uzywac atrybutow `data-testid` lub tekstu widocznego.
- Nie zamykanie przegladarki/strony w Teardown - wycieki procesow przegladarki
  na agentach CI i spowolnienie kolejnych uruchomien.
- Mieszanie SeleniumLibrary i Browser Library w jednym pliku testowym bez
  wyraznego powodu - roznia sie skladnia keywordow (np. Click vs Click Button)
  i utrudnia to utrzymanie kodu.
- Testowanie na jednej przegladarce i zakladanie, ze wynik bedzie identyczny
  wszedzie - warto uruchamiac krytyczne testy tez na firefox/webkit.

## 5. Ulepszenia z AI
- Wklej fragment HTML/DOM elementu do Claude/ChatGPT i poproś o zaproponowanie
  stabilnego selektora (najlepiej opartego na `data-testid` lub roli ARIA).
- Poproś AI o przepisanie testu z SeleniumLibrary na Browser Library (Playwright)
  - AI dobrze mapuje odpowiedniki keywordow (`Input Text` -> `Fill Text` itp.).
- Uzyj AI do analizy zrzutu ekranu bledu (np. z `report.html`) i opisania,
  ktory element mogl nie zaladowac sie na czas.
- Generuj przez AI warianty negatywnych scenariuszy logowania (puste pola,
  niepoprawne dane, znaki specjalne) na podstawie jednego dzialajacego testu.
- Poproś AI o wygenerowanie Page Object (Resource file) na podstawie listy
  lokalizatorow i opisu akcji na stronie.

## Zadanie na dzis
Zainstaluj Browser Library (`pip install robotframework-browser` + `rfbrowser init`)
i napisz test, ktory otworzy dowolna publiczna strone z formularzem logowania,
wypelni pola przez `Fill Text`, kliknie przycisk przez `Click`, a nastepnie
zweryfikuje komunikat bledu (bo dane beda niepoprawne) przez `Get Text`.
Dodaj [Tags] web oraz Suite Setup/Teardown zarzadzajace przegladarka.

================================================================================

# Dzien 7 (2026-07-30, czwartek) - RequestsLibrary do testow API

## 1. Temat dnia
Testowanie API (REST) w Robot Framework za pomoca biblioteki RequestsLibrary -
sesje HTTP, wysylanie zadan GET/POST/PUT/DELETE oraz walidacja odpowiedzi JSON.

## 2. Kluczowe zasady i najlepsze praktyki
- Zawsze zaczynaj od `Create Session` z nazwa i adresem bazowym (base URL) -
  pozwala to potem odwolywac sie tylko do sciezek wzglednych (`/posts`) zamiast
  powtarzac pelny adres URL w kazdym zadaniu.
- Preferuj nowsze keywordy `GET On Session`, `POST On Session`, `PUT On Session`,
  `DELETE On Session` (dzialajace na sesji) zamiast starszych `Get Request`/
  `Post Request` bez sesji - latwiej zarzadzac wspolnymi naglowkami i auth.
- Uzywaj parametru `expected_status` zamiast recznego sprawdzania kodu - RF
  od razu zglosi czytelny blad, jesli API zwroci inny status niz oczekiwany
  (np. `expected_status=200` albo `expected_status=201`).
- Odpowiedz (`response`) to obiekt Pythona - dostep do danych przez
  `${response.json()}`, `${response.status_code}`, `${response.text}`; do
  walidacji struktury JSON korzystaj z biblioteki Collections
  (`Dictionary Should Contain Key`, `Get From Dictionary`).
- Tokeny, klucze API i dane logowania trzymaj w zmiennych srodowiskowych lub
  pliku konfiguracyjnym (`%{API_TOKEN}`), nigdy jako literal w pliku .robot -
  szczegolnie jesli repo jest publiczne.
- Ustawiaj rozsadny `timeout` przy tworzeniu sesji, zeby test nie wisial
  w nieskonczonosc przy niedostepnym serwerze/API.

## 3. Przykladowy kod
```robotframework
*** Settings ***
Library           RequestsLibrary
Library           Collections

*** Variables ***
${BASE_URL}       https://jsonplaceholder.typicode.com

*** Test Cases ***
Pobranie listy postow (GET)
    [Documentation]    Test pobiera liste zasobow i weryfikuje odpowiedz.
    [Tags]    api    smoke
    Create Session    jsonplaceholder    ${BASE_URL}    timeout=10
    ${response}=    GET On Session    jsonplaceholder    /posts    expected_status=200
    Should Be Equal As Integers    ${response.status_code}    200
    ${liczba}=    Get Length    ${response.json()}
    Should Be True    ${liczba} > 0

Utworzenie nowego zasobu (POST)
    [Documentation]    Test wysyla dane JSON i weryfikuje utworzenie zasobu.
    [Tags]    api
    Create Session    jsonplaceholder    ${BASE_URL}    timeout=10
    &{dane}=    Create Dictionary    title=Testowy post    body=Tresc testowa    userId=1
    ${response}=    POST On Session    jsonplaceholder    /posts    json=${dane}    expected_status=201
    Dictionary Should Contain Key    ${response.json()}    id
    Should Be Equal    ${response.json()}[title]    Testowy post

Obsluga bledu 404 (negatywny scenariusz)
    [Documentation]    Sprawdza reakcje API na nieistniejacy zasob.
    [Tags]    api    negative
    Create Session    jsonplaceholder    ${BASE_URL}    timeout=10
    ${response}=    GET On Session    jsonplaceholder    /posts/99999    expected_status=404
    Should Be Equal As Integers    ${response.status_code}    404
```

## 4. Najczestsze bledy
- Uzywanie starszych keywordow bez sesji (`Get Request`, `Post Request`) tam,
  gdzie lepiej pasuje sesja z `Create Session` - trudniej wtedy zarzadzac
  wspolnymi naglowkami (np. tokenem auth) dla wielu zadan w tym samym pliku.
- Brak `expected_status` i poleganie tylko na tym, ze RF sam zglosi wyjatek -
  bez jawnego assercji trudno odroznic blad testu od faktycznej awarii API.
- Zakladanie, ze klucz zawsze istnieje w odpowiedzi JSON (`${response.json()}[klucz]`)
  bez wczesniejszej weryfikacji `Dictionary Should Contain Key` - nieczytelny
  KeyError zamiast jasnego komunikatu bledu.
- Hardkodowanie tokenow/API keys bezposrednio w pliku .robot zamiast w
  zmiennych srodowiskowych lub bezpiecznym pliku konfiguracyjnym.
- Brak `timeout` przy `Create Session` - test moze wisiec bardzo dlugo, gdy
  API nie odpowiada, zamiast szybko zakonczyc sie czytelnym bledem.

## 5. Ulepszenia z AI
- Poproś Claude/Copilot o wygenerowanie testow RequestsLibrary na podstawie
  specyfikacji OpenAPI/Swagger danego API (endpointy, metody, kody statusow).
- Wklej przykladowa odpowiedz JSON z API i poproś AI o wygenerowanie asercji
  Collections (`Dictionary Should Contain Key`, `Get From Dictionary`) do
  walidacji jej struktury.
- Uzyj AI do wygenerowania przykladowych danych testowych (payloadow JSON) do
  testow POST/PUT na podstawie opisu modelu danych.
- Poproś AI o przeksztalcenie polecenia `curl` na odpowiadajacy mu fragment
  .robot z RequestsLibrary (Create Session + odpowiedni keyword On Session).
- Wklej log bledu nieudanego testu API i poproś AI o wskazanie, czy problem
  lezy po stronie testu (zla asercja/URL) czy po stronie API (np. 500/timeout).

## Zadanie na dzis
Napisz suite korzystajacy z RequestsLibrary i Collections, ktory: utworzy
sesje do publicznego API (np. jsonplaceholder.typicode.com), wykona test GET
zwracajacy liste i zweryfikuje jej dlugosc oraz strukture pierwszego elementu,
wykona test POST tworzacy nowy zasob z walidacja kodu statusu (expected_status)
i zawartosci odpowiedzi, oraz dodatkowo test negatywny sprawdzajacy odpowiedz
API na zadanie o nieistniejacy zasob (np. status 404).

================================================================================
# Dzien 8 (2026-07-31, piatek) - Dane testowe i Data-Driven Testing z Templates

## 1. Temat dnia
Dzisiejszy temat to obsluga danych testowych oraz Data-Driven Testing (DDT)
w Robot Framework przy uzyciu sekcji `[Template]`. Zamiast pisac osobny Test
Case dla kazdego zestawu danych, definiujemy jeden szablonowy Keyword, a w
sekcji `*** Test Cases ***` podajemy tylko kolejne wiersze danych wejsciowych.
Znacznie skraca to testy i ulatwia utrzymanie logiki w jednym miejscu.

## 2. Kluczowe zasady i najlepsze praktyki
- `[Template]` w Test Case (lub globalnie w `*** Settings ***` jako
  `Test Template`) zamienia kazdy wiersz danych pod nazwa testu w osobne
  wywolanie wskazanego Keywordu - kazdy wiersz to de facto oddzielny "test".
- Kazdy wiersz danych w takim Test Case powinien miec czytelna nazwe
  (np. "Logowanie z pustym haslem"), bo to ona pojawia sie w log.html/report.html
  jako identyfikator konkretnego przypadku.
- Dane testowe warto trzymac oddzielnie od logiki: w plikach CSV/Excel
  wczytywanych przez `OperatingSystem`/biblioteki zewnetrzne, w zmiennych
  (`*** Variables ***`) lub w osobnym Resource file - Keyword szablonowy
  ma tylko przyjmowac argumenty, a nie zawierac dane na sztywno.
- Do parametryzacji wielu argumentow na wiersz uzywaj tabel z wyrownanymi
  kolumnami (dobra czytelnosc) oraz `...` do kontynuacji dlugich wierszy.
- Laczenie `[Template]` z `[Tags]` per wiersz (np. `[Tags]` w kolejnych
  liniach) pozwala selektywnie uruchamiac tylko wybrane przypadki danych.
- Gdy jeden wiersz danych zawiedzie, pozostale wiersze i tak sie wykonaja -
  to zaleta DDT (pelny raport wszystkich przypadkow), ale trzeba o tym
  pamietac projektujac asercje.

## 3. Przykladowy kod
```robotframework
*** Settings ***
Documentation     Przyklad Data-Driven Testing z uzyciem [Template].
Library           Collections

*** Test Cases ***
Walidacja logowania dla roznych danych
    [Template]    Sprawdz wynik logowania
    login=admin      haslo=admin123     oczekiwany=SUKCES
    login=admin      haslo=zlehaslo     oczekiwany=BLAD
    login=${EMPTY}   haslo=admin123     oczekiwany=BLAD
    login=admin      haslo=${EMPTY}     oczekiwany=BLAD
    login=${EMPTY}   haslo=${EMPTY}     oczekiwany=BLAD

*** Keywords ***
Sprawdz wynik logowania
    [Arguments]    ${login}    ${haslo}    ${oczekiwany}
    ${wynik}=    Zaloguj    ${login}    ${haslo}
    Should Be Equal    ${wynik}    ${oczekiwany}

Zaloguj
    [Arguments]    ${login}    ${haslo}
    IF    "${login}" == "" or "${haslo}" == ""
        RETURN    BLAD
    END
    IF    "${login}" == "admin" and "${haslo}" == "admin123"
        RETURN    SUKCES
    END
    RETURN    BLAD
```

Przyklad z danymi z pliku CSV (wczytanie i iteracja przez `FOR`):
```robotframework
*** Settings ***
Library    OperatingSystem
Library    String

*** Test Cases ***
Test z danymi z pliku CSV
    ${zawartosc}=    Get File    ${CURDIR}/dane_logowania.csv
    @{wiersze}=      Split To Lines    ${zawartosc}
    FOR    ${wiersz}    IN    @{wiersze}
        @{dane}=    Split String    ${wiersz}    ,
        Sprawdz wynik logowania    @{dane}
    END
```

## 4. Najczestsze bledy
- Wpisywanie danych testowych na sztywno wewnatrz Keywordu szablonowego
  zamiast przekazywania ich jako argumenty - traci sie caly sens DDT.
- Brak czytelnych nazw wierszy w Test Case z `[Template]` - w raporcie
  widac wtedy tylko nieopisane, trudne do zidentyfikowania przypadki.
- Mieszanie w jednym Template roznej liczby/kolejnosci argumentow w
  poszczegolnych wierszach, co prowadzi do myloncych bledow o niezgodnej
  liczbie parametrow.
- Ignorowanie faktu, ze kazdy wiersz to osobny "test" w raporcie - przy
  duzej liczbie danych log.html/report.html moze zrobic sie nieczytelny,
  jesli nie pogrupuje sie danych sensownie (np. po tagach).
- Brak walidacji formatu danych wejsciowych (np. z CSV) przed uzyciem ich
  w Keywordzie - literowka w danych zewnetrznych powoduje trudny do
  namierzenia falszywy blad testu.

## 5. Ulepszenia z AI
- Poproś AI o wygenerowanie kompletu przypadkow brzegowych (edge cases) do
  tabeli `[Template]` na podstawie opisu reguly biznesowej (np. walidacja
  formularza logowania czy rejestracji).
- Wklej istniejacy zbior danych testowych (np. arkusz/CSV) i poproś AI o
  przeksztalcenie go w gotowa tabele `[Template]` w skladni Robot Framework.
- Wykorzystaj AI do wygenerowania danych syntetycznych (np. losowe adresy
  email, numery telefonow, kombinacje pol) do zasilenia testow DDT.
- Poproś AI o zaproponowanie brakujacych kombinacji danych (macierz
  parametrow), ktore mogly zostac pominiete w reczne pisanej tabeli.
- Uzyj AI do przejrzenia istniejacego zestawu duplikatow Test Casow i
  zaproponowania ich konsolidacji w jeden Test Case z `[Template]`.

## Zadanie na dzis
Zamien trzy oddzielne, powtarzalne Test Case'y (np. testujace rejestracje
uzytkownika z roznymi danymi wejsciowymi) na jeden Test Case korzystajacy z
`[Template]`. Dodaj co najmniej 5 wierszy danych obejmujacych przypadki
pozytywne i negatywne (np. puste pole, zly format email, za krotkie haslo),
nadaj kazdemu wierszowi czytelna nazwe i uruchom test z flaga `--test`, aby
sprawdzic, ze kazdy wiersz jest widoczny jako osobny przypadek w raporcie.

================================================================================
# Dzien 9 (2026-08-01, sobota) - Tagowanie i selektywne uruchamianie testow

## 1. Temat dnia
Tagowanie testow ([Tags]) oraz selektywne uruchamianie ich przy pomocy flag
`--include` / `--exclude` / `--test` / `--suite` w Robot Framework.

## 2. Kluczowe zasady i najlepsze praktyki
- Tagi nadaje sie w [Tags] na poziomie Test Case lub globalnie przez
  Force Tags/Default Tags w sekcji *** Settings ***.
- Uzywaj krotkich, spojnych nazw tagow (np. smoke, regression, api, slow) -
  ustal konwencje nazewnictwa dla calego zespolu i trzymaj sie jej.
- Tagi mozna laczyc logicznie przy uruchamianiu: `--include tagA --exclude
  tagB` albo wyrazenia typu `smokeANDapi`.
- Warto tagowac testy wg priorytetu (critical/high/low), obszaru (login,
  checkout) i typu (smoke, regression, slow) - to umozliwia elastyczne
  odpalanie podzbiorow w CI/CD.
- Unikaj tagow "na wyrost" bez sensu biznesowego - kazdy tag powinien
  odpowiadac na pytanie "po co bede filtrowac tym tagiem".

## 3. Przyklad kodu (.robot)
```robotframework
*** Settings ***
Documentation     Przyklad tagowania testow logowania
Force Tags        regression

*** Test Cases ***
Poprawne logowanie
    [Documentation]    Test smoke - krytyczna sciezka logowania
    [Tags]    smoke    critical    login
    Log    Symulacja poprawnego logowania

Logowanie z blednym haslem
    [Tags]    negative    login
    Log    Symulacja logowania z blednym haslem

Logowanie - test wydajnosciowy
    [Tags]    slow    performance
    Log    Symulacja dlugotrwalego testu logowania
```

Uruchomienie tylko testow smoke:
```bash
robot --include smoke tests/
```

Uruchomienie regresji z pominieciem wolnych testow:
```bash
robot --include regression --exclude slow tests/
```

Uruchomienie po wyrazeniu logicznym (smoke ORAZ login):
```bash
robot --include smokeANDlogin tests/
```

## 4. Najczestsze bledy
- Niespojne nazwy tagow (np. "Smoke", "smoke", "SMOKE-TEST" jako rozne
  tagi) - RF traktuje je jako oddzielne wartosci, filtr nie zlapie wszystkich.
  Robot Framework normalizuje wielkosc liter/spacje przy porownaniu tagow,
  ale mieszanie konwencji (myslnik vs podkreslnik) i tak utrudnia utrzymanie.
- Brak dokumentacji tagow - nikt w zespole nie wie co dany tag oznacza.
- Tagowanie "na sztywno" pojedynczych testow zamiast uzycia Force
  Tags/Default Tags dla calego pliku, gdy tag dotyczy calej suity.
- Zbyt duza liczba unikalnych tagow bez planu - trudno potem filtrowac
  sensowne podzbiory testow.
- Mylenie --include/--exclude z selekcja po nazwie (--test, --suite) -
  to inne mechanizmy i mozna je laczyc, ale trzeba rozumiec roznice.

## 5. Ulepszenia z AI
- Poproszenie Claude/Copilot o zaproponowanie spojnej konwencji nazewnictwa
  tagow dla calego projektu na podstawie listy istniejacych testow.
- Wklejenie pliku .robot i poproszenie AI o audyt tagow: wykrycie
  duplikatow, literowek i niespojnosci (np. "smoke" vs "smoke-test").
- Generowanie komend CI (np. dla GitHub Actions) z odpowiednia kombinacja
  --include/--exclude na podstawie opisu "chce odpalac tylko smoke na PR,
  a pelna regresje w nocy".
- Automatyczne generowanie dokumentacji tagow (README/wiki) na podstawie
  analizy uzytych tagow w calym repozytorium testowym.
- Analiza logow z uruchomienia (log.html/output.xml) przez AI w celu
  zaproponowania, ktore testy warto oznaczyc jako "slow" lub "flaky" na
  podstawie czasu wykonania i historii wynikow.

================================================================================
# Dzien 10 (2026-08-02) - Resource files i modularizacja testow

## 1. Temat dnia
Pliki .resource (Resource files) oraz modularizacja projektu testowego - jak
dzielic wspolne slowa kluczowe, zmienne i importy na osobne pliki, aby testy
byly czytelne, ponownie uzywalne i latwe w utrzymaniu.

## 2. Kluczowe zasady i najlepsze praktyki
- Wspolne slowa kluczowe i zmienne wydzielaj do plikow z rozszerzeniem
  .resource (nie .robot) - jasno oddziela to "biblioteke" testowa od
  wykonywalnych przypadkow testowych.
- Importuj plik zasobow przez `Resource    sciezke/do/pliku.resource` w
  sekcji *** Settings *** - sciezki podawaj wzgledem lokalizacji biedzacego
  pliku (mozna uzyc `${CURDIR}` dla pewnosci niezaleznie od katalogu
  uruchomienia).
- Trzymaj strukture katalogow logicznie: np. `resources/` (keywordy wspolne),
  `resources/pages/` (Page Object - lokalizatory i akcje per strona/ekran),
  `tests/` (przypadki testowe), `data/` (dane testowe).
- Jeden plik .resource powinien miec jedna odpowiedzialnosc (np.
  `login_keywords.resource`, `api_common.resource`) - unikaj jednego
  "worka" ze wszystkimi keywordami calego projektu.
- Unikaj cyklicznych importow (plik A importuje B, B importuje A) - RF
  zglosi blad parsowania; planuj hierarchie importow od ogolu do szczegolu.

## 3. Przyklad kodu (.robot)
Plik `resources/login_keywords.resource`:
```robotframework
*** Settings ***
Documentation     Wspolne slowa kluczowe zwiazane z logowaniem.
Library           Collections

*** Variables ***
${VALID_USER}        standard_user
${VALID_PASSWORD}     secret_sekrit

*** Keywords ***
Zaloguj Uzytkownika
    [Arguments]    ${login}=${VALID_USER}    ${haslo}=${VALID_PASSWORD}
    Log    Logowanie jako: ${login}
    Set Suite Variable    ${AKTUALNY_UZYTKOWNIK}    ${login}

Uzytkownik Powinien Byc Zalogowany
    Should Not Be Empty    ${AKTUALNY_UZYTKOWNIK}
    Log    Zalogowany uzytkownik: ${AKTUALNY_UZYTKOWNIK}
```

Plik `tests/login_tests.robot`:
```robotframework
*** Settings ***
Documentation     Testy logowania korzystajace z modulu resource.
Resource          ${CURDIR}/../resources/login_keywords.resource

*** Test Cases ***
Poprawne logowanie standardowym uzytkownikiem
    [Tags]    smoke    login
    Zaloguj Uzytkownika
    Uzytkownik Powinien Byc Zalogowany

Logowanie innym uzytkownikiem
    [Tags]    login
    Zaloguj Uzytkownika    login=admin_user    haslo=admin_pass123
    Uzytkownik Powinien Byc Zalogowany
```

## 4. Najczestsze bledy
- Uzywanie rozszerzenia .robot dla plikow zasobow zamiast .resource - dziala,
  ale myli czytelnikow (plik .robot sugeruje wykonywalne testy).
- Sciezki wzgledne liczone od katalogu uruchomienia `robot`, a nie od
  lokalizacji pliku - powoduje bledy "Import failed" przy uruchamianiu z
  innego katalogu; lekarstwo to `${CURDIR}`.
- Nadmierne "spłaszczanie" - jeden gigantyczny resource ze wszystkimi
  keywordami projektu utrudnia znalezienie czegokolwiek i zwieksza ryzyko
  konfliktow nazw.
- Duplikowanie tych samych keywordow w kilku resource'ach zamiast wydzielenia
  wspolnego, bardziej ogolnego pliku bazowego.
- Brak Documentation w plikach .resource - kolejny czlonek zespolu nie wie,
  do czego sluzy dany modul bez czytania calego kodu.

## 5. Ulepszenia z AI
- Poproszenie Claude/Copilot o zaproponowanie podzialu istniejacego,
  rozroslego pliku .robot na osobne resource'y wedlug odpowiedzialnosci
  (logowanie, koszyk, platnosci).
- Wklejenie kilku plikow testowych i poproszenie AI o wykrycie duplikatow
  keywordow, ktore mozna wyciagnac do wspolnego resource.
- Generowanie szkieletu struktury katalogow projektu (resources/, tests/,
  data/) na podstawie opisu aplikacji, ktora bedzie testowana.
- Automatyczne generowanie Documentation dla plikow .resource na podstawie
  analizy uzytych w nich Keywords.
- Poproszenie AI o audyt importow (Resource) pod katem cyklicznych zaleznosci
  lub nieuzywanych/zbednych importow w projekcie.

## Zadanie na dzis
Wydziel z dowolnego posiadanego pliku .robot (lub z przykladu powyzej) co
najmniej dwa wspolne slowa kluczowe do osobnego pliku .resource, zaimportuj
go w pliku testowym przez `Resource    ${CURDIR}/...` i uruchom test, aby
sprawdzic, ze modularyzacja dziala poprawnie.

================================================================================
# Dzien 11 (2026-08-03) - Custom Keywords i tworzenie wlasnych bibliotek w Pythonie

## 1. Temat dnia
Tworzenie wlasnych slow kluczowych (Custom Keywords) - zarowno w plikach
.robot/.resource, jak i jako pelnoprawne biblioteki napisane w Pythonie -
gdy standardowe biblioteki nie wystarczaja do wyrazenia logiki testowej.

## 2. Kluczowe zasady i najlepsze praktyki
- Custom Keyword w RF-owym .resource wystarcza, gdy logika to zlozenie
  istniejacych keywordow; biblioteke w Pythonie pisz dopiero, gdy potrzebujesz
  wlasnych obliczen, integracji z zewnetrznym API/SDK albo wydajnosci
  niedostepnej w czystym RF.
- Kazda klasa biblioteki w Pythonie powinna miec atrybut
  `ROBOT_LIBRARY_SCOPE` ('GLOBAL', 'SUITE' lub 'TEST') - okresla on, jak
  dlugo zyje instancja biblioteki i czy stan jest dzielony miedzy testami.
- Metody publiczne klasy staja sie slowami kluczowymi automatycznie (nazwa
  metody `zaloguj_uzytkownika` -> keyword `Zaloguj Uzytkownika`) - metody
  pomocnicze, ktore NIE maja byc keywordem, poprzedzaj podkreslnikiem `_`.
- Dokumentuj kazdy keyword docstringiem - trafia on wprost do log.html/
  Libdoc i jest jedynym zrodlem wiedzy dla reszty zespolu bez czytania kodu.
- Rzucaj wyjatki (`raise AssertionError(...)`) zamiast zwracac True/False -
  RF traktuje kazdy wyjatek jako FAIL testu, co jest zgodne z konwencja
  asercji w calym frameworku.

## 3. Przyklad kodu
Biblioteka Pythona `libraries/CalculatorLibrary.py`:
```python
from robot.api.deco import keyword, library


@library(scope="TEST", auto_keywords=False)
class CalculatorLibrary:
    """Prosta biblioteka RF demonstrujaca wlasne slowa kluczowe."""

    def __init__(self):
        self._historia = []

    @keyword("Dodaj Liczby")
    def dodaj_liczby(self, a, b):
        """Dodaje dwie liczby i zapisuje wynik w historii operacji."""
        wynik = float(a) + float(b)
        self._historia.append(wynik)
        return wynik

    @keyword("Ostatni Wynik Powinien Byc Rowny")
    def ostatni_wynik_powinien_byc_rowny(self, oczekiwany):
        if not self._historia:
            raise AssertionError("Brak zapisanej historii operacji.")
        ostatni = self._historia[-1]
        if ostatni != float(oczekiwany):
            raise AssertionError(
                f"Oczekiwano {oczekiwany}, otrzymano {ostatni}"
            )
```

Uzycie w pliku testowym `tests/calculator_tests.robot`:
```robotframework
*** Settings ***
Documentation     Test wlasnej biblioteki Pythona z zestawem keywordow.
Library           ${CURDIR}/../libraries/CalculatorLibrary.py

*** Test Cases ***
Dodawanie dwoch liczb dodatnich
    [Tags]    unit    custom-library
    Dodaj Liczby    3    4
    Ostatni Wynik Powinien Byc Rowny    7

*** Keywords ***
Zaloguj I Zweryfikuj Naglowek
    [Documentation]    Przyklad keyworda RF zlozonego z innych keywordow.
    [Arguments]    ${login}
    Log    Logowanie jako ${login}
    Page Should Contain    Witaj, ${login}
```

## 4. Najczestsze bledy
- Brak `ROBOT_LIBRARY_SCOPE` - domyslnie kazda instancja zyje tylko na czas
  jednego testu (TEST), co bywa zaskoczeniem, gdy oczekuje sie stanu
  wspoldzielonego w calym suite.
- Zwracanie `False`/komunikatu bledu zamiast rzucenia wyjatku - test konczy
  sie PASS mimo faktycznej porazki logicznej.
- Nazywanie metod pomocniczych bez podkreslnika `_` - przypadkowo staja sie
  publicznymi keywordami i zaśmiecaja liste dostepnych slow kluczowych.
- Mieszanie logiki biznesowej z asercjami w jednym keywordzie - utrudnia to
  ponowne uzycie (raz chcemy tylko pobrac wartosc, raz od razu ja zweryfikowac).
- Brak testow jednostkowych dla samej biblioteki Pythona - bledy w keywordach
  wychodza dopiero na duzych, kosztownych testach E2E.

## 5. Ulepszenia z AI
- Poproś Claude/Copilot o wygenerowanie szkieletu klasy biblioteki RF (z
  `ROBOT_LIBRARY_SCOPE`, dekoratorem `@keyword`, docstringami) na podstawie
  opisu potrzebnej funkcjonalnosci w jezyku naturalnym.
- Wklej istniejacy, rozrosniety Custom Keyword napisany w .robot i poproś AI
  o ocene, czy powinien zostac przepisany na biblioteke Pythona (np. ze
  wzgledu na zlozonosc logiki lub potrzebe obslugi wyjatkow).
- Uzyj AI do wygenerowania testow jednostkowych (pytest) dla wlasnej
  biblioteki Pythona, niezaleznie od testow .robot, ktore z niej korzystaja.
- Poproś o wygenerowanie dokumentacji Libdoc/README dla biblioteki na
  podstawie kodu zrodlowego, wraz z przykladami uzycia kazdego keyworda.
- Wykorzystaj AI do przegladu nazewnictwa keywordow pod katem spojnosci
  (np. czy wszystkie asercje zaczynaja sie od "Powinien/Powinna Byc").

## Zadanie na dzis
Napisz prosta biblioteke Pythona z co najmniej dwoma slowami kluczowymi
(jedno wykonujace operacje, drugie weryfikujace jej wynik przez rzucenie
wyjatku przy niepowodzeniu), zaimportuj ja w pliku .robot przez
`Library    ${CURDIR}/...` i uruchom test potwierdzajacy, ze oba keywordy
dzialaja poprawnie.

================================================================================
# Dzien 12 (2026-08-04) - Raportowanie i logi (log.html, report.html)

## 1. Temat dnia
Jak Robot Framework generuje wyniki testow - roznica miedzy output.xml,
log.html i report.html, sterowanie poziomem szczegolowosci logu oraz
dolaczanie dodatkowych informacji (zrzuty ekranu, wlasne wpisy) do raportu.

## 2. Kluczowe zasady i najlepsze praktyki
- output.xml to zrodlo prawdy - zawiera wszystkie dane z przebiegu testow;
  log.html i report.html sa generowane na jego podstawie (recznie przez
  narzedzie `rebot`, albo automatycznie po kazdym uruchomieniu `robot`).
- report.html to podsumowanie wysokiego poziomu (statystyki, tagi, PASS/FAIL
  per suite) - log.html to szczegolowy, klikalny zapis kazdego kroku i
  slowa kluczowego, wraz z argumentami i wartosciami zwracanymi.
- Poziom logowania ustawiaj przez `--loglevel` (domyslnie INFO) - w CI
  warto uzywac WARN/INFO, a szczegoly DEBUG/TRACE wlaczac tylko przy
  diagnozowaniu konkretnego problemu, bo TRACE potrafi wygenerowac
  ogromne pliki.
- Do wlasnych wpisow w logu uzywaj `Log` / `Log To Console` z jawnym
  poziomem (`Log    Wiadomosc    level=DEBUG`), a nie tylko domyslnego
  INFO - ulatwia to filtrowanie logu po fakcie.
- Nigdy nie loguj sekretow (hasel, tokenow) w postaci jawnej - RF domyslnie
  nie maskuje wartosci; jesli trzeba, uzyj `Log    ${haslo}    level=NONE`
  albo argumentu `--variable HASLO:*****` przy wywolaniu, ewentualnie
  wlasnej logiki maskujacej w Custom Keywordzie.

## 3. Przyklad kodu
```robotframework
*** Settings ***
Documentation     Przyklad logowania wlasnych informacji i zrzutu ekranu.
Library           OperatingSystem

*** Test Cases ***
Logowanie wlasnych wpisow do log.html
    [Documentation]    Pokazuje rozne poziomy logowania i Log To Console.
    [Tags]    reporting
    Log    Rozpoczynam test logowania danych    level=INFO
    Log    Szczegoly techniczne widoczne tylko przy --loglevel DEBUG    level=DEBUG
    Log To Console    Ta linia trafia od razu na konsole, nie tylko do logu
    Log Many    Wpis pierwszy    Wpis drugi    Wpis trzeci

Zapis zrzutu przy niepowodzeniu
    [Documentation]    Wymusza blad, aby pokazac wpis FAIL w log.html.
    [Tags]    reporting
    ${plik_istnieje}=    Run Keyword And Return Status
    ...    File Should Exist    ${CURDIR}/brak_takiego_pliku.txt
    Run Keyword If    not ${plik_istnieje}
    ...    Log    Plik nie istnieje - oczekiwane w tym przykladzie    level=WARN
    Should Be True    ${plik_istnieje}
    ...    msg=Plik testowy powinien istniec przed dalszymi krokami
```

Przydatne polecenia CLI do pracy z wynikami (poza kodem .robot):
```
robot --loglevel DEBUG --outputdir results tests/
rebot --merge --output merged.xml results/output1.xml results/output2.xml
rebot --log log.html --report report.html --loglevel WARN results/output.xml
```

## 4. Najczestsze bledy
- Ustawianie `--loglevel TRACE` na stale w CI - logi robia sie ogromne i
  ciezkie do przegladania, a build spowalnia przez sam zapis na dysk.
- Logowanie hasel/tokenow wprost w `Log` lub w argumentach keywordow -
  trafiaja one do log.html w postaci jawnej i moga wyciec przez artefakty CI.
- Mylenie report.html z log.html - szukanie szczegolow bledu w report.html,
  gdzie jest tylko podsumowanie, zamiast w log.html.
- Nieprzechowywanie output.xml jako artefaktu CI - bez niego nie da sie
  pozniej wygenerowac log.html/report.html ani zmergowac wynikow przez rebot.
- Ignorowanie kolorowych sekcji WARN/ERROR w konsoli podczas przebiegu -
  test moze przejsc (PASS), a mimo to zawierac istotne ostrzezenia w logu.

## 5. Ulepszenia z AI
- Wklejenie fragmentu log.html (lub tresci bledu z konsoli) do Claude/
  ChatGPT z prosba o wskazanie najbardziej prawdopodobnej przyczyny FAIL-a
  na podstawie stack trace'u i argumentow keywordu.
- Poproszenie AI o wygenerowanie czytelnego podsumowania z duzego
  output.xml/report.html (np. "ktore suity maja najwiecej FAIL, jaki jest
  sredni czas trwania testu") zamiast recznego przegladania.
- Uzycie AI do zaproponowania miejsc w kodzie testowym, gdzie warto dodac
  `Log`/`Log To Console` z konkretnym poziomem, aby ulatwic przyszle
  debugowanie bez zasmiecania logu w trybie normalnym.
- Poproszenie AI o przejrzenie testow pod katem logowania danych
  wrazliwych (hasel, tokenow, PII) i zaproponowanie sposobu ich maskowania.
- Wygenerowanie przez AI polecenia `rebot` do zmergowania wynikow z wielu
  rownoleglych przebiegow (np. z Pabot) w jeden spojny log.html/report.html.

## Zadanie na dzis
Uruchom dowolny test z parametrem `--loglevel DEBUG`, dodaj do niego wlasny
`Log` na poziomie DEBUG oraz `Log To Console`, a nastepnie porownaj, co
widac w log.html przy `--loglevel INFO` a co dodatkowo pojawia sie przy
`--loglevel DEBUG`.

================================================================================
# Dzien 13 (2026-08-05) - Obsluga wyjatkow i Run Keyword And Expect Error

## 1. Temat dnia
Obsluga oczekiwanych bledow w Robot Framework - jak testowac scenariusze
negatywne (np. walidacje, wyjatki API/aplikacji) przy pomocy keywordow
`Run Keyword And Expect Error`, `Run Keyword And Ignore Error` oraz bloku
`TRY/EXCEPT` (RF 5+), zamiast traktowac kazdy blad jako niepowodzenie testu.

## 2. Kluczowe zasady i najlepsze praktyki
- `Run Keyword And Expect Error` jest wlasciwym wyborem, gdy TEST MA
  potwierdzic, ze dany keyword faktycznie rzuca oczekiwany blad - pierwszy
  argument to wzorzec komunikatu bledu (mozna uzyc `*`/`glob` lub prefiksu
  `EQUALS:`/`STARTS:`/`REGEXP:` dla precyzyjnego dopasowania).
- `Run Keyword And Ignore Error` uzywaj tylko tam, gdzie blad NIE powinien
  przerywac testu, ale nie chcesz go tez jawnie weryfikowac (np. opcjonalne
  sprzatanie) - w przeciwienstwie do Expect Error, nie sprawdza tresci bledu.
- Od RF 5 preferuj natywny blok `TRY ... EXCEPT ... ELSE ... FINALLY ... END`
  zamiast starszych keywordow tam, gdzie potrzebna jest bardziej rozbudowana
  logika (kilka warunkow bledu, dostep do wiadomosci przez `AS ${zmienna}`).
- Nie "lykaj" bledow bez potrzeby - kazde uzycie Ignore Error/TRY-EXCEPT
  powinno miec jasny powod biznesowy, w przeciwnym razie test przestaje
  wykrywac prawdziwe regresje (falszywy PASS mimo realnej awarii).
- Dopasowuj wzorzec bledu na tyle scisle, by test lapal wlasciwy wyjatek,
  ale na tyle luzno (np. przez `*` na koncu), by drobna zmiana tresci
  komunikatu (np. numer wiersza) nie psula testu bez potrzeby.

## 3. Przykladowy kod
```robotframework
*** Settings ***
Documentation     Przyklady obslugi oczekiwanych i nieoczekiwanych bledow.
Library           Collections

*** Test Cases ***
Oczekiwany blad przy dzieleniu przez zero
    [Documentation]    Weryfikuje, ze keyword rzuca wlasciwy, oczekiwany wyjatek.
    [Tags]    exceptions
    Run Keyword And Expect Error    ZeroDivisionError: *
    ...    Podziel Liczby    10    0

Ignorowanie bledu przy opcjonalnym sprzataniu
    [Documentation]    Blad w kroku sprzatajacym nie powinien przerywac testu.
    [Tags]    exceptions
    Run Keyword And Ignore Error    Usun Plik Tymczasowy    ${CURDIR}/brak.txt
    Log    Test kontynuowany niezaleznie od wyniku sprzatania

Obsluga bledu przez TRY/EXCEPT (RF 5+)
    [Documentation]    Nowoczesna skladnia obslugi wyjatkow z dostepem do tresci bledu.
    [Tags]    exceptions
    TRY
        Podziel Liczby    10    0
    EXCEPT    ZeroDivisionError: *    AS    ${blad}
        Log    Zlapano oczekiwany blad: ${blad}
    ELSE
        Fail    Oczekiwano bledu dzielenia przez zero, ale go nie bylo
    FINALLY
        Log    Ten blok wykonuje sie zawsze, niezaleznie od wyniku
    END

*** Keywords ***
Podziel Liczby
    [Arguments]    ${a}    ${b}
    ${wynik}=    Evaluate    ${a} / ${b}
    RETURN    ${wynik}

Usun Plik Tymczasowy
    [Arguments]    ${sciezka}
    OperatingSystem.Remove File    ${sciezka}
```

## 4. Najczestsze bledy
- Nadmierne uzywanie `Run Keyword And Ignore Error` "na wszelki wypadek" -
  test przechodzi (PASS) mimo realnej awarii, bo blad zostal po cichu
  zignorowany zamiast zweryfikowany.
- Zbyt scisle dopasowanie tresci bledu w `Run Keyword And Expect Error`
  (caly komunikat 1:1) - test pada przy najmniejszej zmianie sformulowania
  wyjatku, mimo ze logika biznesowa sie nie zmienila.
- Brak weryfikacji TRESCI bledu, a jedynie faktu jego wystapienia - test
  przejdzie takze wtedy, gdy rzucony zostanie zupelnie inny, niezwiazany
  wyjatek niz oczekiwany.
- Mieszanie starych keywordow (Run Keyword And Expect Error/Ignore Error)
  z nowym TRY/EXCEPT bez wyraznego powodu w tym samym projekcie - utrudnia
  to spojnosc i czytelnosc kodu dla calego zespolu.
- Brak bloku FINALLY/teardownu przy krokach wymagajacych sprzatania (np.
  zamkniecie polaczenia) wewnatrz TRY - w razie bledu zasob moze zostac
  niezwolniony.

## 5. Ulepszenia z AI
- Poproś Claude/Copilot o wygenerowanie testow negatywnych weryfikujacych
  konkretne, oczekiwane komunikaty bledow na podstawie opisu walidacji
  biznesowej (np. "za krotkie haslo", "brak wymaganego pola").
- Wklej stack trace/wyjatek z logu i poproś AI o zaproponowanie wlasciwego
  wzorca dopasowania (`glob`, `REGEXP:`, `STARTS:`) do `Run Keyword And
  Expect Error`, tak by nie byl ani zbyt scisly, ani zbyt luzny.
- Uzyj AI do przeglądu istniejacych testow pod katem nadużywania `Run
  Keyword And Ignore Error` i wskazania miejsc, gdzie blad powinien byc
  jawnie zweryfikowany zamiast zignorowany.
- Poproś o przepisanie starszej obslugi bledow (Run Keyword And Expect
  Error) na nowoczesny blok TRY/EXCEPT/FINALLY zgodny z RF 5+.
- Wykorzystaj AI do wygenerowania macierzy przypadkow bledowych (jakie
  wyjatki moze rzucic dana funkcja/API) na podstawie opisu logiki lub
  dokumentacji endpointu.

## Zadanie na dzis
Napisz Custom Keyword, ktory rzuca wyjatek przy niepoprawnych danych
wejsciowych (np. pusty argument lub wartosc ujemna), a nastepnie napisz
trzy testy: jeden weryfikujacy oczekiwany blad przez `Run Keyword And
Expect Error` z dopasowaniem wzorca, jeden korzystajacy z bloku
TRY/EXCEPT/ELSE/FINALLY z odczytem tresci bledu do zmiennej, oraz jeden
pozytywny scenariusz, w ktorym blad NIE wystepuje.

================================================================================
# Dzien 14 (2026-08-06) - Zmienne srodowiskowe i pliki konfiguracyjne

## 1. Temat dnia
Zmienne srodowiskowe (`%{ZMIENNA}`) oraz pliki konfiguracyjne (`--variablefile`,
pliki `.py`/`.yaml`/`.robot` z argumentami) - jak oddzielic dane wrazliwe i
srodowiskowo-zalezne (URL, loginy, tokeny, timeouty) od logiki testow.

## 2. Kluczowe zasady i najlepsze praktyki
- Nigdy nie trzymaj hasel, tokenow ani sekretow jako literalow w plikach
  .robot - odczytuj je przez `%{NAZWA_ZMIENNEJ}` z prawdziwych zmiennych
  srodowiskowych (ustawianych np. w CI/CD jako "secrets").
- Do wartosci ktore roznia sie miedzy srodowiskami (dev/staging/prod: base
  URL, dane logowania, timeouty) uzywaj oddzielnych plikow zmiennych
  (`--variablefile config_dev.py`, `--variablefile config_prod.yaml`)
  zamiast twardego kodowania w suite'ach.
- Keyword `Get Environment Variable` (OperatingSystem) pozwala odczytac
  zmienna srodowiskowa z poziomu kodu testu, z opcjonalna wartoscia
  domyslna, gdy zmienna nie jest ustawiona.
- Preferuj pliki `.yaml`/`.py` jako zrodlo zmiennych zamiast prostych plikow
  `.robot` - latwiej je parsowac programistycznie, walidowac i wersjonowac,
  a `.py` pozwala budowac wartosci dynamicznie (np. losowe dane, sciezki).
- Sekrety wstrzykuj wylacznie w czasie uruchomienia (`robot --variable
  API_TOKEN:%{CI_TOKEN}` lub zmienna srodowiskowa odczytana przez keyword) -
  nigdy nie commituj plikow z prawdziwymi hasłami do repozytorium (uzyj
  `.gitignore` dla plikow `secrets_local.py`/`.env`).

## 3. Przykladowy kod
```robotframework
*** Settings ***
Documentation     Przyklad uzycia zmiennych srodowiskowych i pliku konfiguracyjnego.
Library           OperatingSystem
Variables         config/${ENV}.py

*** Variables ***
${DEFAULT_TIMEOUT}    10s

*** Test Cases ***
Logowanie z danymi z konfiguracji srodowiska
    [Documentation]    Adres i dane logowania pochodza z pliku config/${ENV}.py,
    ...    a token API ze zmiennej srodowiskowej ustawionej poza repozytorium.
    [Tags]    config
    ${token}=    Get Environment Variable    API_TOKEN    default=BRAK_TOKENU
    Should Not Be Equal    ${token}    BRAK_TOKENU
    ...    msg=Brak zmiennej srodowiskowej API_TOKEN - ustaw ja przed uruchomieniem testow
    Log    Laczenie z ${BASE_URL} jako ${USERNAME}
    Log    Timeout requestow: ${DEFAULT_TIMEOUT}
```

```python
# config/dev.py - plik zmiennych dla srodowiska "dev"
BASE_URL = "https://dev.example.com"
USERNAME = "tester_dev"
```

```bash
# Uruchomienie z podaniem srodowiska i wstrzyknieciem sekretu ze zmiennej systemowej
export API_TOKEN="tajny-token-z-sejfu-cicd"
robot --variable ENV:dev tests/
```

## 4. Najczestsze bledy
- Trzymanie prawdziwych hasel/tokenow bezposrednio w sekcji `*** Variables
  ***` lub w plikach `.robot` commitowanych do repozytorium.
- Brak wartosci domyslnej/walidacji przy `Get Environment Variable` - test
  konczy sie niejasnym bledem zamiast czytelnego komunikatu "brak zmiennej
  X, ustaw ja przed uruchomieniem".
- Twarde zakodowanie adresu URL/srodowiska w testach, przez co przelaczenie
  miedzy dev/staging/prod wymaga recznej edycji wielu plikow zamiast jednej
  flagi `--variablefile`/`--variable ENV:...`.
- Mieszanie wielu zrodel tej samej zmiennej (raz z CLI, raz z pliku, raz na
  sztywno w kodzie) bez jasnej hierarchii - trudno przewidziec, ktora
  wartosc "wygra" podczas uruchomienia.
- Brak pliku `.gitignore` dla lokalnych plikow z sekretami (`secrets_local.py`,
  `.env`), co grozi przypadkowym wypchnieciem danych wrazliwych do repo.

## 5. Ulepszenia z AI
- Poproś AI o wygenerowanie szablonu plikow konfiguracyjnych dla kilku
  srodowisk (dev/staging/prod) na podstawie listy potrzebnych zmiennych.
- Wklej istniejacy suite z hardcodowanymi danymi i poproś o refaktoryzacje -
  wyodrebnienie sekretow/adresow do zmiennych srodowiskowych i pliku
  konfiguracyjnego.
- Uzyj AI do wygenerowania checklisty bezpieczenstwa (co NIE powinno trafic
  do repozytorium: klucze API, hasla, tokeny) i skonfigurowania `.gitignore`.
- Poproś o wygenerowanie dokumentacji projektu opisujacej, jakie zmienne
  srodowiskowe sa wymagane do uruchomienia testow lokalnie i w CI/CD.
- Wykorzystaj AI do przegladu pipeline'u CI/CD pod katem bezpiecznego
  przekazywania sekretow (np. jako maskowane "secrets", a nie zmienne
  jawne w logach).

## Zadanie na dzis
Stworz plik `config/dev.py` z przykladowymi zmiennymi (`BASE_URL`,
`USERNAME`), zaimportuj go w suite przez `Variables`, a nastepnie napisz
test odczytujacy dodatkowa zmienna srodowiskowa (np. `API_TOKEN`) przez
`Get Environment Variable` z wartoscia domyslna i jawna walidacja jej
obecnosci przed uzyciem w tescie.

================================================================================
# Dzien 15 (2026-08-08, sobota) - RPA z Robot Framework

## 1. Temat dnia
Robot Framework nie sluzy tylko do testowania - dzieki sekcji `*** Tasks ***`
oraz bibliotece `rpaframework` mozna nim tworzyc procesy RPA (Robotic Process
Automation): automatyzacje biznesowe dzialajace bez nadzoru czlowieka, np.
przetwarzanie plikow Excel, wysylka maili, obsluga stron webowych czy API.

## 2. Kluczowe zasady i najlepsze praktyki
- Uzywaj sekcji `*** Tasks ***` zamiast `*** Test Cases ***`, gdy plik
  opisuje proces biznesowy, a nie test - RF automatycznie przelacza sie
  w tryb RPA (terminologia "Task" zamiast "Test" w raportach); pliku NIE
  wolno mieszac obu sekcji naraz.
- Korzystaj z bibliotek `rpaframework` (np. `RPA.Excel.Files`, `RPA.Tables`,
  `RPA.HTTP`, `RPA.FileSystem`, `RPA.Email.ImapSmtp`, `RPA.Browser.Selenium`)
  zamiast pisac integracje od zera - pokrywaja najczestsze potrzeby RPA
  (pliki biurowe, poczta, PDF, chmura).
- Procesy RPA dzialaja bez nadzoru - kazdy krok, ktory moze zawiesc (odczyt
  pliku, request HTTP, logowanie), obudowuj blokiem `TRY/EXCEPT`, aby
  pojedynczy blad nie przerwal calego batcha, tylko pozwolil kontynuowac
  przetwarzanie kolejnych elementow (np. kolejnych wierszy Excela).
- Loguj kazdy istotny krok procesu (`Log To Console`, `Log`) - w RPA nikt
  nie obserwuje wykonania na zywo, wiec log.html/report.html to jedyne
  zrodlo prawdy przy diagnozowaniu bledow produkcyjnych.
- Dane wrazliwe (loginy, hasla, klucze API) trzymaj w sejfie sekretow
  (np. `RPA.Robocorp.Vault`) lub zmiennych srodowiskowych - nigdy w kodzie
  procesu, ktory czesto trafia do wspolnego repozytorium/orkiestratora.

## 3. Przykladowy kod
```robotframework
*** Settings ***
Documentation     Proces RPA: wczytanie zamowien z Excela, przetworzenie i eksport do CSV.
Library           RPA.Excel.Files
Library           RPA.Tables
Library           RPA.FileSystem

*** Variables ***
${INPUT_FILE}     ${CURDIR}${/}dane${/}zamowienia.xlsx
${OUTPUT_FILE}    ${CURDIR}${/}output${/}duze_zamowienia.csv
${PROG_KWOTY}     1000

*** Tasks ***
Przetworz zamowienia z pliku Excel
    [Documentation]    Filtruje zamowienia powyzej progu kwotowego i zapisuje wynik do CSV.
    TRY
        Open Workbook    ${INPUT_FILE}
        ${zamowienia}=    Read Worksheet As Table    header=${TRUE}
    EXCEPT    AS    ${blad}
        Log To Console    Nie udalo sie wczytac pliku ${INPUT_FILE}: ${blad}
        RETURN
    FINALLY
        Close Workbook
    END

    ${duze_zamowienia}=    Filter Table By Column
    ...    ${zamowienia}    Kwota    >    ${PROG_KWOTY}
    Create File    ${OUTPUT_FILE}
    Export Table To CSV Doc    ${duze_zamowienia}    ${OUTPUT_FILE}
    Log To Console    Zapisano ${duze_zamowienia.row_count} zamowien do ${OUTPUT_FILE}
```

```bash
# Uruchomienie procesu RPA (RF automatycznie wykrywa tryb po sekcji *** Tasks ***)
robot --outputdir wyniki tasks/przetworz_zamowienia.robot

# Wymuszenie trybu RPA jawna flaga (przydatne przy mieszanych projektach)
robot --rpa tasks/przetworz_zamowienia.robot
```

## 4. Najczestsze bledy
- Mieszanie `*** Test Cases ***` i `*** Tasks ***` w jednym pliku .robot -
  Robot Framework dopuszcza tylko jeden typ sekcji na plik.
- Brak obslugi bledow w krokach zaleznych od zewnetrznych zasobow (plik,
  strona, API) - jeden niedostepny plik przerywa caly batch zamiast
  przejsc do kolejnego rekordu z odpowiednim logiem.
- Zbyt skape logowanie kroków procesu - przy awarii na produkcji (bez
  mozliwosci podejrzenia wykonania na zywo) trudno ustalic, na ktorym
  etapie i dlaczego proces sie zatrzymal.
- Trzymanie hasel/kluczy API wprost w kodzie procesu lub w plikach
  konfiguracyjnych commitowanych do repo, zamiast w sejfie sekretow.
- Traktowanie RPA jak zwyklego testu (jeden `Fail` konczy caly przebieg)
  zamiast projektowania procesu tak, by kontynuowal dzialanie mimo
  pojedynczych bledow tam, gdzie logika biznesowa na to pozwala.

## 5. Ulepszenia z AI
- Poproś AI o wygenerowanie szkieletu `*** Tasks ***` na podstawie opisu
  procesu biznesowego w jezyku naturalnym (np. "wczytaj Excela, odfiltruj
  wiersze, wyslij mailem raport").
- Wklej nazwy kolumn z rzeczywistego pliku Excel/CSV i poproś AI o
  wygenerowanie keywordow `RPA.Tables`/`RPA.Excel.Files` do ich filtrowania
  i walidacji.
- Uzyj AI do zaprojektowania strategii obslugi bledow (TRY/EXCEPT, retry,
  kontynuacja mimo bledu pojedynczego rekordu) dla procesu dzialajacego
  bez nadzoru.
- Poproś o wygenerowanie pliku `robot.yaml`/`conda.yaml` pod wdrozenie
  procesu w orkiestratorze (np. Robocorp/Sema4.ai) na podstawie listy
  uzywanych bibliotek.
- Wykorzystaj AI do przegladu logow z nieudanego przebiegu RPA i
  zaproponowania miejsca, w ktorym warto dodac dodatkowe logowanie lub
  blok TRY/EXCEPT.

## Zadanie na dzis
Stworz plik `.robot` z sekcja `*** Tasks ***` (nie `Test Cases`), ktory
przy pomocy `RPA.Excel.Files`/`RPA.Tables` wczytuje przykladowy plik Excel,
filtruje wiersze wedlug wybranej kolumny i zapisuje wynik do CSV. Obuduj
odczyt pliku blokiem `TRY/EXCEPT/FINALLY`, tak by blad odczytu byl
zalogowany, a nie przerywal calego procesu w nieczytelny sposob.

================================================================================
# Dzien 16 (2026-08-09, niedziela) - Dobre praktyki (Page Object Pattern, nazewnictwo, struktura projektu)

## 1. Temat dnia
Organizacja projektu testow Robot Framework tak, by rosl w sposob czytelny
i latwy w utrzymaniu: wzorzec Page Object dla testow webowych, spojne
nazewnictwo testow/keywordow oraz przejrzysta struktura katalogow.

## 2. Kluczowe zasady i najlepsze praktyki
- Oddzielaj warstwy: pliki `.robot` z Test Cases opisuja CO ma byc
  sprawdzone (jezyk biznesowy), pliki `.resource` z keywordami "strony"
  opisuja JAK to zrobic (locatory, kliknięcia) - test nigdy nie powinien
  zawierac surowego locatora (np. `id=submit-btn`).
- Jeden plik `.resource` = jedna strona/komponent aplikacji (Page Object) -
  np. `pages/login_page.resource`, `pages/dashboard_page.resource` -
  zmiana UI danej strony wymaga edycji tylko jednego pliku.
- Stosuj spojna konwencje nazewnicza: Test Cases w jezyku biznesowym
  ("Uzytkownik Loguje Sie Poprawnymi Danymi"), keywordy akcji w formie
  czasownikowej ("Wprowadz Dane Logowania"), keywordy asercji zaczynaj od
  "Powinien/Powinna Byc" ("Strona Glowna Powinna Byc Widoczna").
- Struktura katalogow oddziela odpowiedzialnosci, np.:
  `tests/` (przypadki testowe), `resources/pages/` (Page Objects),
  `resources/keywords/` (keywordy wspoldzielone), `libraries/` (biblioteki
  Pythona), `variables/` (dane/konfiguracja), `results/` (output RF).
- Trzymaj locatory jako zmienne w sekcji `*** Variables ***` danego Page
  Objectu (np. `${LOGIN_BUTTON}    id=submit-btn`), a nie wprost w
  wywolaniach keywordow - jedna zmiana ID elementu = jedna linia do edycji.

## 3. Przyklad kodu
Page Object `resources/pages/login_page.resource`:
```robotframework
*** Settings ***
Library           Browser

*** Variables ***
${USERNAME_INPUT}    id=username
${PASSWORD_INPUT}    id=password
${LOGIN_BUTTON}       id=submit-btn
${WELCOME_HEADER}     css=h1.welcome

*** Keywords ***
Wprowadz Dane Logowania
    [Documentation]    Wypelnia formularz logowania podanymi danymi.
    [Arguments]    ${login}    ${haslo}
    Fill Text    ${USERNAME_INPUT}    ${login}
    Fill Text    ${PASSWORD_INPUT}    ${haslo}
    Click    ${LOGIN_BUTTON}

Strona Glowna Powinna Byc Widoczna
    [Documentation]    Weryfikuje, ze po zalogowaniu widoczny jest naglowek powitalny.
    Get Text    ${WELCOME_HEADER}    ==    Witaj ponownie!
```

Test korzystajacy z Page Objectu `tests/login.robot`:
```robotframework
*** Settings ***
Documentation     Testy logowania oparte o wzorzec Page Object.
Resource          ../resources/pages/login_page.resource
Suite Setup       Open Browser    https://example.test/login    chromium

*** Test Cases ***
Uzytkownik Loguje Sie Poprawnymi Danymi
    [Tags]    smoke    login
    Wprowadz Dane Logowania    jan.kowalski    TajneHaslo123
    Strona Glowna Powinna Byc Widoczna
```

Przykladowa struktura katalogow projektu:
```text
projekt-testow/
├── tests/
│   └── login.robot
├── resources/
│   ├── pages/
│   │   └── login_page.resource
│   └── keywords/
│       └── common.resource
├── libraries/
│   └── CustomLibrary.py
├── variables/
│   └── env_dev.py
└── results/
```

## 4. Najczestsze bledy
- Wpisywanie locatorow bezposrednio w Test Cases zamiast w Page Objectach -
  jedna zmiana w UI wymusza edycje wielu plikow testowych naraz.
- Nazwy testow techniczne zamiast biznesowych (np. "Test 1", "TC_LOGIN_02")
  - raport z takiego przebiegu nic nie mowi osobie spoza zespolu QA.
  - "Uzytkownik Loguje Sie Poprawnymi Danymi" mowi wszystko bez czytania kodu.
- Jeden ogromny plik `.resource` ze wszystkimi keywordami calej aplikacji -
  brak podzialu na strony/komponenty utrudnia znalezienie wlasciwego
  keyworda i zwieksza ryzyko konfliktow przy pracy zespolowej.
- Duplikowanie tych samych keywordow w wielu plikach zamiast wydzielenia
  ich do wspolnego `.resource` - poprawka bledu wymaga wtedy zmiany w
  kilku miejscach, a latwo o jedno pominiete.
- Mieszanie logiki asercji z logika akcji w jednym keywordzie Page Objectu
  - utrudnia to ponowne uzycie samej akcji bez natychmiastowej weryfikacji.

## 5. Ulepszenia z AI
- Wklej fragment HTML/DOM strony i poproś AI o wygenerowanie sekcji
  `*** Variables ***` z proponowanymi, stabilnymi locatorami (preferuj
  `id`/`data-testid` nad kruchymi selektorami CSS opartymi na klasach).
- Poproś AI o propozycje struktury katalogow projektu na podstawie opisu
  aplikacji (liczba modulow, czy sa API i UI, czy potrzebne jest RPA).
- Wykorzystaj AI do przegladu istniejacego pliku `.robot` pod katem
  spojnosci nazewnictwa testow i keywordow wedlug ustalonej konwencji
  zespolu.
- Poproś o refaktoryzacje "grubego" pliku `.resource" na kilka mniejszych
  Page Objectow, jeden na komponent/strone aplikacji.
- Uzyj AI do wygenerowania checklisty code-review dla nowych plikow .robot
  (np. "czy sa locatory wprost w tescie?", "czy nazwa testu jest biznesowa?").

## Zadanie na dzis
Wez dowolny istniejacy test webowy (lub napisz nowy prosty test logowania)
i rozbij go na warstwy: przenies wszystkie locatory oraz kroki interakcji
ze strona do osobnego pliku `.resource` (Page Object), a w pliku testowym
zostaw wylacznie wywolania keywordow w jezyku biznesowym. Nazwij test i
keywordy zgodnie z konwencjami z tej notatki.

================================================================================
# Dzien 17 (2026-08-11, wtorek) - CI/CD integracja Robot Framework (Jenkins, GitHub Actions)

## 1. Temat dnia
Wlaczenie testow Robot Framework do pipeline'u CI/CD - automatyczne
uruchamianie ich przy kazdym pushu/PR (np. w GitHub Actions lub Jenkins),
publikowanie wynikow (`report.html`, `log.html`, `output.xml`) jako
artefaktow oraz oznaczanie builda jako failed, gdy testy nie przejda.

## 2. Kluczowe zasady i najlepsze praktyki
- Uruchamiaj testy w CI dokladnie tak samo jak lokalnie - trzymaj wersje
  Pythona i zaleznosci w `requirements.txt`/`Pipfile.lock`, zeby uniknac
  efektu "u mnie dziala".
- Zawsze archiwizuj/publikuj wyniki (`output.xml`, `log.html`,
  `report.html`) jako artefakty builda - bez nich debugowanie awarii w CI
  polega na zgadywaniu na podstawie samego stdout.
- Rozdzielaj testy tagami na szybki smoke-set uruchamiany przy kazdym
  pushu (`--include smoke`) oraz pelny regresyjny zestaw uruchamiany
  cyklicznie/noca - skraca to czas oczekiwania na feedback z PR.
- Kod wyjscia `robot` (`rc`) odzwierciedla liczbe testow zakonczonych
  niepowodzeniem (0 = wszystkie OK) - CI powinno interpretowac `rc != 0`
  jako czerwony build, bez dodatkowego parsowania logow.
- Uzywaj `rebot` do laczenia wynikow z kilku rownoleglych wykonan
  (np. shardowanych po tagach lub przegladarkach) w jeden zbiorczy
  `report.html`/`log.html` przed opublikowaniem jako artefakt.

## 3. Przyklad kodu
Workflow GitHub Actions `.github/workflows/robot-tests.yml`:
```yaml
name: Robot Framework Tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instaluj zaleznosci
        run: pip install -r requirements.txt

      - name: Uruchom testy Robot Framework
        run: |
          robot --outputdir results --include smoke tests/

      - name: Opublikuj wyniki jako artefakt
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: robot-results
          path: results/
```

Odpowiednik jako `Jenkinsfile` (Pipeline deklaratywny):
```groovy
pipeline {
    agent any
    stages {
        stage('Instaluj zaleznosci') {
            steps {
                sh 'pip install -r requirements.txt'
            }
        }
        stage('Testy Robot Framework') {
            steps {
                sh 'robot --outputdir results --include smoke tests/'
            }
        }
    }
    post {
        always {
            archiveArtifacts artifacts: 'results/*.html,results/*.xml'
            robot outputPath: 'results'
        }
    }
}
```

## 4. Najczestsze bledy
- Brak publikowania `log.html`/`report.html` jako artefaktow - po awarii
  w CI dostepny jest tylko surowy stdout, co znaczaco wydluza diagnoze.
- Krok publikacji wynikow bez `if: always()` (GitHub Actions) lub bloku
  `post { always {...} }` (Jenkins) - gdy testy zakoncza sie niepowodzeniem,
  wyniki wcale nie zostana zarchiwizowane, bo poprzedni krok przerwal job.
- Uruchamianie w CI calego, pelnego zestawu testow (w tym wolnych E2E) przy
  kazdym malym pushu - spowalnia to petle feedbacku dla zespolu zamiast
  najpierw puscic szybki smoke-set.
- Poleganie wylacznie na kodzie wyjscia `robot` bez ustawienia limitu czasu
  (`timeout`) joba w CI - zawieszony test (np. brak elementu na stronie w
  testach webowych) moze blokowac runner bez konca.
- Rozne wersje bibliotek/Pythona lokalnie i w CI (brak zamrozonych
  zaleznosci) - test przechodzi lokalnie, a w CI failuje z niezwiazanym
  bledem importu.

## 5. Ulepszenia z AI
- Wklej opis swojego projektu (jezyk, struktura katalogow, biblioteki RF) i
  poproś AI o wygenerowanie startowego pliku `robot-tests.yml` lub
  `Jenkinsfile` dopasowanego do tej struktury.
- Poproś AI o przeanalizowanie logu z nieudanego builda CI (fragment
  `output.xml`/stdout) i wskazanie, ktory krok/test zawiodl oraz
  prawdopodobna przyczyne.
- Uzyj AI do zaproponowania podzialu testow na tagi/shardy (np. smoke vs
  regression, per-modul) tak, by rownolegle joby CI konczyly sie w
  podobnym czasie.
- Poproś o wygenerowanie kroku CI publikujacego `report.html` jako
  komentarz w pull requescie (np. link do artefaktu lub podsumowanie
  liczby testow OK/FAIL).
- Wykorzystaj AI do przegladu istniejacego pipeline'u CI/CD pod katem
  brakujacego `if: always()`/`post { always {...} }` czy braku limitu czasu
  na job.

## Zadanie na dzis
Dodaj do swojego repozytorium (lub lokalnego projektu testowego) plik
workflow GitHub Actions, ktory: instaluje zaleznosci z `requirements.txt`,
uruchamia testy Robot Framework z tagiem `smoke`, a nastepnie publikuje
katalog wynikowy (`log.html`, `report.html`, `output.xml`) jako artefakt
builda - rowniez wtedy, gdy testy zakoncza sie niepowodzeniem.

================================================================================
# Dzien 18 (2026-08-14, piatek) - Debugowanie testow (RIDE, Robotidy, rflint)

## 1. Temat dnia
Narzedzia wspierajace pisanie, formatowanie i statyczna analize testow Robot
Framework: edytor RIDE, formater Robotidy oraz lintery rflint/robocop. Lapia
bledy i niespojnosci zanim test w ogole zostanie uruchomiony.

## 2. Kluczowe zasady i najlepsze praktyki
- Robotidy to oficjalny, deterministyczny formater kodu .robot/.resource -
  uruchamiaj go automatycznie (pre-commit hook, CI) zamiast recznie pilnowac
  wciec, wyrownania kolumn i spacji.
- rflint (i nowoczesniejszy robocop) wykrywaja problemy jakosciowe: zbyt
  dlugie test case'y, brak [Documentation], zduplikowane nazwy slow
  kluczowych, zbyt glebokie zagniezdzenie - traktuj je jak pylint/eslint dla
  Robot Framework.
- RIDE (Robot Framework IDE) bywa przydatny do szybkiego podgladu struktury
  suite'ow, ale jest juz mniej rozwijany - do codziennej pracy lepiej sprawdza
  sie VS Code z rozszerzeniem Robot Framework Language Server (podswietlanie
  skladni, przejscie do definicji keywordu, podpowiedzi).
- Do debugowania pojedynczego, zawieszajacego sie testu uzywaj flagi
  --loglevel DEBUG oraz izolacji jednego testu (--test "Nazwa testu"), zamiast
  odpalac caly, dlugi suite od nowa wielokrotnie.
- Wlacz linter i formatter do CI jako osobny, szybki krok (--check w
  Robotidy) - dzieki temu bledy stylu wylapiesz zanim testy w ogole zaczna
  sie uruchamiac.

## 3. Przykladowy kod
Konfiguracja w pyproject.toml:
```toml
[tool.robotidy]
line_length = 120
spacecount = 4

[tool.robocop]
include = ["all"]
exclude = ["line-too-long"]
```

Uruchomienie formatowania i lintowania lokalnie:
```bash
# Sformatuj caly katalog testow (nadpisuje pliki)
robotidy tests/

# Sprawdz formatowanie bez modyfikacji plikow (np. w CI)
robotidy --check tests/

# Statyczna analiza jakosci kodu
robocop tests/

# Starszy, klasyczny linter
rflint tests/
```

Przyklad testu z celowym problemem, ktory zlapie linter:
```robotframework
*** Test Cases ***
test1
    Log    Za krotka, nieopisowa nazwa testu bez [Documentation] i [Tags]
    Click Element    id=btn
    Click Element    id=btn
    Click Element    id=btn
```
robocop/rflint zglosza tu m.in.: brak dokumentacji, brak tagow, nazwe
niezgodna z konwencja oraz zduplikowane, powtarzajace sie kroki.

Uruchomienie testu w trybie debug z pelnym logiem:
```bash
robot --loglevel DEBUG --outputdir results tests/logowanie.robot
```

## 4. Najczestsze bledy
- Ignorowanie ostrzezen lintera "bo test i tak przechodzi" - dlug techniczny
  narasta i po miesiacach nikt nie wie, ktore ostrzezenia sa wazne.
- Reczne poprawianie formatowania zamiast uruchomienia Robotidy - prowadzi do
  niespojnego stylu w zespole i niepotrzebnych konfliktow w code review.
- Debugowanie przez wielokrotne uruchamianie calego, dlugiego test suite'u
  zamiast wyizolowania jednego testu flaga --test "Nazwa testu" lub tagiem.
- Poleganie wylacznie na report.html przy diagnozie bledu - to log.html
  (poziom DEBUG) pokazuje faktyczne argumenty i wartosci zmiennych krok po
  kroku.
- Brak robotidy --check / robocop w CI - problemy jakosciowe wykrywane sa
  dopiero "recznie", przy okazji code review, zamiast automatycznie.

## 5. Ulepszenia z AI
- Wklej output z robocop/rflint do AI i poproś o priorytetyzacje: ktore
  ostrzezenia sa krytyczne (np. duplikacja logiki), a ktore kosmetyczne.
- Poproś AI o wygenerowanie konfiguracji pyproject.toml dla Robotidy/robocop
  dopasowanej do konwencji juz istniejacych w Twoim repozytorium.
- Wklej fragment log.html (lub jego tekstowy odpowiednik z --loglevel DEBUG)
  z nieudanego testu i poproś AI o wskazanie prawdopodobnej przyczyny awarii.
- Uzyj AI do refaktoryzacji testu wykrytego przez linter jako "zbyt dlugi" -
  poproś o podzial na mniejsze, wielokrotnego uzytku Custom Keywords.
- Poproś AI o wygenerowanie pre-commit hooka (.pre-commit-config.yaml)
  uruchamiajacego robotidy --check i robocop przed kazdym commitem.

## Zadanie na dzis
Zainstaluj robotidy i robocop (pip install robotframework-tidy
robotframework-robocop), uruchom je na dowolnym wlasnym pliku .robot (lub na
przykladzie z tej notatki), a nastepnie napraw co najmniej dwa zglosone
problemy - jeden formatowania (Robotidy) i jeden jakosciowy (robocop/rflint).

================================================================================
# Dzień 19 (2026-08-15, sobota) - Podstawy składni Robot Framework (runda 2: sekcja Settings, importy, embedded arguments)

## 1. Temat dnia
Cykl 18 tematów właśnie się zamknął, więc wracamy do podstaw składni - tym
razem z naciskiem na to, co często pomijane na starcie: pełną zawartość
sekcji *** Settings ***, importy (Library/Resource/Variables), metadane
suite'u, komentarze oraz "embedded arguments" w nazwach słów kluczowych.

## 2. Kluczowe zasady i najlepsze praktyki
- Sekcja *** Settings *** to nie tylko Library - znajdziesz tam też Resource
  (import plików .resource), Variables (import zmiennych z pliku .py/.yaml),
  Metadata (dodatkowe informacje w report.html) oraz Suite Setup/Teardown,
  Test Setup/Teardown i Test Tags (dawniej Force Tags) stosowane globalnie
  dla całego suite'u.
- "Embedded arguments" pozwalają wplatać argumenty bezpośrednio w nazwę
  słowa kluczowego (np. `Zaloguj jako ${rola}`) - kod czyta się wtedy jak
  zdanie, bez osobnej listy argumentów pod spodem.
- Styl BDD (Given/When/Then/And/But) to wyłącznie konwencja nazewnicza -
  Robot Framework automatycznie "obcina" te prefiksy przy dopasowywaniu
  słowa kluczowego, więc `Given uzytkownik jest zalogowany` i `uzytkownik
  jest zalogowany` to ten sam keyword.
- Osobna sekcja *** Comments *** (rzadko używana) pozwala trzymać większe
  bloki notatek poza kodem testów - w praktyce częściej wystarczy zwykłe #.
- Kolejność importów w Settings ma znaczenie przy konfliktach nazw słów
  kluczowych - biblioteka/resource zaimportowana później "przykrywa" wcześ-
  niejszą przy tej samej nazwie keywordu, więc unikaj duplikatów nazw.

## 3. Przykładowy kod
```robotframework
*** Settings ***
Documentation     Przykład pelnej sekcji Settings z importami i metadanymi.
Metadata          Autor         Zespol QA
Metadata          Wersja        1.2
Library           Collections
Resource          resources/logowanie.resource
Variables         config/dane_testowe.py
Suite Setup       Log    Start suite'u
Suite Teardown    Log    Koniec suite'u
Test Tags         smoke    regresja

*** Test Cases ***
Given uzytkownik chce sie zalogowac
    [Documentation]    Test pisany w stylu BDD, korzysta z embedded arguments.
    Given uzytkownik jest na stronie logowania
    When Zaloguj jako ${ADMIN}
    Then uzytkownik widzi ekran glowny

*** Keywords ***
uzytkownik jest na stronie logowania
    Log    Otwieram strone logowania

Zaloguj jako ${rola}
    [Documentation]    Embedded argument ${rola} wplecony w nazwe keywordu.
    Log    Loguje sie jako: ${rola}

uzytkownik widzi ekran glowny
    Log    Sprawdzam obecnosc ekranu glownego
```

## 4. Najczęstsze błędy
- Mylenie Force Tags/Test Tags z tagami na poziomie testu - ustawienie w
  Settings dotyczy WSZYSTKICH testów w pliku, łatwo przypadkiem otagować
  cały suite jednym, zbyt ogólnym tagiem.
- Import dwóch bibliotek/resource'ów z keywordem o identycznej nazwie bez
  jawnego wskazania, z której biblioteki korzystać (`Library.Keyword`) -
  RF zgłasza wtedy błąd niejednoznaczności dopiero przy uruchomieniu.
- Nadużywanie embedded arguments tam, gdzie zwykła lista argumentów byłaby
  czytelniejsza - dobre dla krótkich, zdaniowych keywordów, złe dla
  keywordów z wieloma parametrami.
- Zapominanie, że prefiksy Given/When/Then/And/But są tylko kosmetyczne -
  próba wywołania keywordu z innym prefiksem niż w definicji "nie działa",
  bo ktoś myśli, że to osobne słowa kluczowe.
- Brak Metadata i Documentation na poziomie Settings - report.html traci
  kontekst (kto pisał, po co, jaka wersja), co utrudnia utrzymanie projektu.

## 5. Ulepszenia z AI
- Wklej istniejący plik .robot i poproś AI o wypisanie wszystkich importów
  z sekcji Settings wraz z krótkim opisem, do czego służy każdy z nich -
  przydatne przy wdrażaniu się w nieznany projekt.
- Poproś Claude/Copilot o przepisanie testu z klasycznych nazw keywordów na
  styl BDD (Given/When/Then) lub odwrotnie, w zależności od konwencji
  zespołu.
- Użyj AI do wykrycia konfliktów nazw keywordów między zaimportowanymi
  bibliotekami/resource'ami, zanim RF zgłosi błąd niejednoznaczności.
- Poproś o wygenerowanie keywordu z embedded arguments na podstawie opisu
  słownego (np. "keyword, który przyjmuje rolę użytkownika w nazwie").
- Poproś AI o uzupełnienie brakujących pól Metadata/Documentation w Settings
  na podstawie zawartości i historii commitów pliku testowego.

## Zadanie na dziś
Rozbuduj dowolny swój plik .robot o pełną sekcję *** Settings *** (Documen-
tation, co najmniej jedno Metadata, Test Tags) oraz dodaj jeden keyword z
embedded argument, który wywołasz w teście z dwoma różnymi wartościami
argumentu.

================================================================================
# Dzien 20 (2026-08-16, niedziela) - Slowa kluczowe wbudowane BuiltIn (runda 2: mniej oczywiste, ale przydatne keywordy)

## 1. Temat dnia
Druga runda tematu BuiltIn - tym razem keywordy rzadziej omawiane na starcie,
ale bardzo przydatne w codziennej pracy: oczekiwanie na warunek, wielokrotne
wywolania, konwersje typow, asercje na dlugosci/typie oraz kontrolowane
pomijanie testow.

## 2. Kluczowe zasady i najlepsze praktyki
- Wait Until Keyword Succeeds zamiast recznych petli/Sleep - retry z limitem
  czasu (np. `10x    2s`) jest czytelniejszy i bardziej odporny na
  nierownomierne opoznienia (np. asynchroniczne API/UI) niz stale Sleep.
- Length Should Be, Should Be True i Should Be Equal As Numbers to trzy
  rozne asercje do trzech roznych celow - nie zastepuj ich generycznym
  Should Be Equal, bo komunikat bledu bedzie mniej czytelny w log.html.
- Convert To Integer/Number/String konwertuje jawnie typ zmiennej - dane z
  zewnetrznych zrodel (API, pliki CSV, zmienne srodowiskowe) zawsze
  przychodza jako string i wymagaja jawnej konwersji przed porownaniem.
- Skip / Skip If pozwala warunkowo pominac test (np. na danym srodowisku)
  bez oznaczania go jako FAIL - lepsze niz komentowanie testu lub Pass
  Execution z ukrytym sensem.
- Run Keywords laczy kilka prostych wywolan w jednej linii (rozdzielonych
  `AND`) - wygodne przy krotkich sekwencjach, ale przy wiecej niz 2-3
  krokach czytelniejszy jest zwykly blok wielolinijkowy lub Custom Keyword.

## 3. Przykladowy kod
```robotframework
*** Test Cases ***
Weryfikacja mniej oczywistych keywordow BuiltIn
    [Documentation]    Przyklad Wait Until Keyword Succeeds, konwersji typow i Skip If.
    ${status_code}=    Set Variable    200
    ${status_code}=    Convert To Integer    ${status_code}
    Should Be True    ${status_code} == 200

    Wait Until Keyword Succeeds    5x    1s
    ...    Sprawdz czy zasob jest gotowy

    ${lista}=    Create List    a    b    c
    Length Should Be    ${lista}    3

    Skip If    '${status_code}' != '200'    msg=Test pomijany - inny status niz 200

    Run Keywords    Log    Krok pierwszy
    ...    AND    Log    Krok drugi

*** Keywords ***
Sprawdz czy zasob jest gotowy
    [Documentation]    Symulacja sprawdzenia gotowosci zasobu - w realnym
    ...    scenariuszu byloby to np. zapytanie do API lub sprawdzenie pliku.
    Log    Zasob gotowy
```

## 4. Najczestsze bledy
- Uzywanie Sleep w petli zamiast Wait Until Keyword Succeeds - dluzszy,
  mniej czytelny kod i sztywny czas oczekiwania zamiast dynamicznego retry.
- Porownywanie stringa z liczba bez Convert To Integer/Number - test
  przechodzi lub pada z powodu typu danych, a nie faktycznej logiki.
- Nadmierne uzycie Run Keywords z wieloma krokami w jednej linii - trudne
  do debugowania w log.html, bo wszystkie kroki widac jako jedna pozycja.
- Mylenie Skip z Pass Execution - Skip oznacza test jako pominiety (SKIP),
  Pass Execution wymusza PASS mimo niewykonania reszty kroku, co zaciemnia
  prawdziwy wynik w report.html.
- Brak limitu czasu/liczby prob w Wait Until Keyword Succeeds - ustawienie
  zbyt duzej liczby powtorzen potrafi ukryc realny problem wydajnosciowy
  zamiast go wykryc.

## 5. Ulepszenia z AI
- Wklej fragment testu z reczna petla i Sleep, poproś Claude/Copilot o
  przepisanie go na Wait Until Keyword Succeeds z sensownymi parametrami
  retry dopasowanymi do opisanego scenariusza.
- Poproś AI o wskazanie miejsc w istniejacych testach, gdzie brakuje
  jawnej konwersji typow (Convert To Integer/Number) przed porownaniem.
- Uzyj AI do wygenerowania warunkow Skip If na podstawie opisu, kiedy dany
  test nie powinien byc uruchamiany (np. "pomin na srodowisku produkcyjnym").
- Poproś o wyjasnienie roznicy Skip vs Pass Execution vs Fail w kontekscie
  konkretnego przypadku, gdy nie jest jasne, ktorego uzyc.
- Wklej log.html/output.xml z nieudanym Wait Until Keyword Succeeds i
  poproś AI o analize, czy problem lezy w za krotkim czasie oczekiwania,
  czy w faktycznym bledzie aplikacji.

## Zadanie na dzis
Napisz test, ktory: (1) pobiera wartosc jako string i konwertuje ja na
liczbe przez Convert To Integer, (2) uzywa Wait Until Keyword Succeeds do
wywolania keywordu symulujacego niestabilny zasob, (3) warunkowo pomija
krok koncowy przez Skip If na podstawie wartosci zmiennej.

================================================================================

# Dzien 21 (2026-08-17, poniedzialek) - Zmienne i ich zasiegi (runda 2: skladnia VAR, pliki zmiennych, wyrazenia Python)

## 1. Temat dnia
Druga runda tematu zmiennych - nowoczesna skladnia VAR (Robot Framework 7.0+)
jako zamiennik Set Test/Suite/Global Variable, ladowanie zmiennych z plikow
zewnetrznych (.py/.yaml), wyrazenia Python wewnatrz ${{ }} oraz bezpieczne
sprawdzanie istnienia zmiennej.

## 2. Kluczowe zasady i najlepsze praktyki
- VAR (RF 7.0+) to jedno slowo kluczowe zastepujace Set Variable/Set Test
  Variable/Set Suite Variable/Set Global Variable - zasieg okresla jawny
  argument `scope=LOCAL|TEST|SUITE|GLOBAL` zamiast osobnych keywordow.
- Domyslny zasieg VAR bez podania `scope=` to LOCAL (jak zwykle przypisanie
  ${x}=), wiec przy migracji ze starego kodu latwo przypadkiem zawezic
  zasieg zmiennej, jesli nie doda sie jawnie odpowiedniego `scope=`.
- Zlozone lub duze zestawy danych (np. konfiguracje wielu srodowisk) lepiej
  trzymac w pliku zmiennych .py lub .yaml i ladowac przez
  `Variables    plik.py` w *** Settings *** albo `--variablefile` z linii
  polecen, zamiast rozrastac sekcje *** Variables *** w kazdym suite.
- Wyrazenie ${{ python_expr }} pozwala wykonac dowolny kod Python wprost w
  miejscu uzycia zmiennej (np. list comprehension, arytmetyka, wywolania
  funkcji) - dla prostych obliczen czytelniejsze niz Evaluate + Set
  Variable, ale nadużywane pogarsza czytelnosc pliku .robot.
- Get Variable Value zwraca wartosc domyslna zamiast rzucac wyjatek, gdy
  zmienna nie istnieje, a Variable Should Exist/Should Not Exist sluzy do
  jawnej asercji o obecnosci zmiennej - uzywaj ich zamiast reczne sprawdzanie
  przez Run Keyword And Ignore Error.

## 3. Przykladowy kod
```robotframework
*** Settings ***
Library      Collections
Variables    config/env_data.py

*** Variables ***
${MNOZNIK}    3

*** Test Cases ***
Nowoczesna skladnia VAR i wyrazenia Python
    VAR    ${liczba}    10    scope=TEST
    VAR    @{wyniki}    scope=SUITE

    ${podwojona}=    Evaluate    ${liczba} * ${MNOZNIK}
    VAR    ${kwadraty}    ${{ [x**2 for x in range(5)] }}

    Log    Wynik obliczen: ${podwojona}
    Log    Lista kwadratow: ${kwadraty}

    ${wartosc}=    Get Variable Value    $ZMIENNA_Z_KONFIGA    domyslna_wartosc
    Should Not Be Empty    ${wartosc}

Bezpieczne sprawdzenie istnienia zmiennej
    VAR    ${status_serwisu}    aktywny    scope=GLOBAL
    Variable Should Exist    ${status_serwisu}
    Variable Should Not Exist    ${nieistniejaca_zmienna_konfiguracyjna}
```

```python
# config/env_data.py - plik zmiennych ladowany przez "Variables    config/env_data.py"
ZMIENNA_Z_KONFIGA = "wartosc-z-pliku-py"
SRODOWISKA = {
    "dev": {"url": "https://dev.example.com", "timeout": 10},
    "prod": {"url": "https://example.com", "timeout": 30},
}
```

## 4. Najczestsze bledy
- Uzywanie VAR bez `scope=` tam, gdzie stary kod korzystal z Set Suite/Global
  Variable - zmienna zostaje LOCAL i "znika" w kolejnym kroku/tescie, co
  wyglada jak losowy blad.
- Naduzywanie ${{ python_expr }} do skomplikowanej logiki biznesowej - taki
  kod trudno czytac i debugowac w pliku .robot; zlozona logika powinna
  trafic do Custom Keyword w Pythonie.
- Zakladanie, ze plik zmiennych .py/.yaml zostanie automatycznie przeladowany
  przy zmianie w trakcie wykonania suite'u - jest wczytywany raz, na starcie.
- Mieszanie w jednym repo starej (Set Test Variable) i nowej (VAR) skladni
  bez konsekwencji - utrudnia onboarding i code review, warto ujednolicic
  konwencje w calym projekcie.
- Pomijanie wartosci domyslnej w Get Variable Value, gdy zalezy nam na
  wykryciu literowki w nazwie zmiennej - bez niej dostajemy cicho ${None}
  zamiast czytelnego bledu.

## 5. Ulepszenia z AI
- Wklej fragment starego kodu z Set Test/Suite/Global Variable i poproś
  Claude/Copilot o migracje na skladnie VAR z poprawnie dobranym `scope=`.
- Wklej zlozona strukture danych (np. JSON z konfiguracja srodowisk) i
  poproś AI o wygenerowanie gotowego pliku zmiennych .py lub .yaml.
- Poproś o wyjasnienie, ktore zrodlo zmiennej "wygrywa" w konkretnym
  przypadku (CLI -v, plik zmiennych, sekcja Variables, VAR w kodzie) - AI
  szybko wskaze kolejnosc priorytetow dla podanego scenariusza.
- Uzyj AI do przepisania zagniezdzonego Evaluate na czytelniejsze ${{ }}
  (lub odwrotnie, gdy wyrazenie ${{ }} zrobilo sie zbyt zlozone).
- Poproś o wygenerowanie testow negatywnych z Variable Should Not Exist dla
  krytycznych zmiennych konfiguracyjnych, ktore nie powinny "przeciekac"
  miedzy suite'ami.

## Zadanie na dzis
Napisz test, ktory: (1) ustawia zmienna przez VAR z jawnym `scope=SUITE` i
odczytuje ja w drugim tescie tego samego suite'u, (2) uzywa wyrazenia
${{ }} do przetworzenia listy (np. przefiltrowania lub podniesienia do
kwadratu elementow), (3) sprawdza przez Get Variable Value wartosc
nieistniejacej zmiennej z podana wartoscia domyslna.

================================================================================
# Dzien 22 (2026-08-18, wtorek) - Test/Suite/Task setup i teardown (runda 2: hierarchia suite'ow, [Teardown] na keywordach, zmienne ${TEST_STATUS})

## 1. Temat dnia
Druga runda tematu setup/teardown - mniej oczywiste aspekty: dziedziczenie
Suite Setup/Teardown w hierarchii katalogow (przez __init__.robot), teardown
zdefiniowany bezposrednio na wlasnym slowie kluczowym (nie tylko na tescie),
wbudowane zmienne dostepne wylacznie w teardownie (${TEST_STATUS},
${TEST_MESSAGE}) oraz zachowanie frameworka, gdy Suite Setup zawiedzie.

## 2. Kluczowe zasady i najlepsze praktyki
- Plik __init__.robot umieszczony w katalogu z testami definiuje Suite
  Setup/Suite Teardown dla CALEGO katalogu (suite'u nadrzednego) - wykonuje
  sie raz przed/po wszystkich plikach .robot w tym katalogu, niezaleznie od
  Suite Setup/Teardown zdefiniowanych w poszczegolnych plikach.
- Jesli Suite Setup zakonczy sie bledem, wszystkie testy w danym suite
  (i podsuite'ach) sa automatycznie oznaczane jako nieudane BEZ ich
  faktycznego wykonania - ale Suite Teardown i tak sie wykona, wiec to
  wlasciwe miejsce na sprzatanie zasobow otwartych czesciowo w Setup.
- Od RF 4.0 slowo kluczowe uzytkownika (*** Keywords ***) moze miec wlasny
  [Teardown] - wykonuje sie po kazdym wywolaniu tego keyworda, nawet gdy
  keyword rzuci wyjatek, co pozwala sprzatac zasoby lokalne dla konkretnego
  keyworda niezaleznie od teardownu calego testu.
- W Test Teardown/[Teardown] (ale NIE w Test Setup) dostepne sa specjalne
  zmienne: ${TEST_STATUS} (PASS/FAIL), ${TEST_MESSAGE} (tresc bledu) oraz
  @{TEST_TAGS} - pozwalaja warunkowo wykonywac dodatkowe kroki (np. zrzut
  ekranu) na podstawie wyniku testu bez uzywania Run Keyword If Test Failed.
- Opcja `--SkipTeardownOnExit` pomija wszystkie teardowny przy przerwaniu
  wykonania (np. Ctrl+C) - przydatna przy debugowaniu, ale w CI/CD nie
  powinna byc domyslnie wlaczona, bo zostawia niezamkniete zasoby (np.
  przegladarki, polaczenia sieciowe).

## 3. Przykladowy kod
```robotframework
*** Settings ***
Library      SeleniumLibrary
Suite Setup       Log    Setup calego katalogu z __init__.robot (przyklad)
Test Teardown     Zaloguj wynik testu

*** Test Cases ***
Test wykorzystujacy TEST_STATUS w teardownie
    [Documentation]    Teardown w Settings automatycznie widzi wynik testu.
    Should Be Equal    1    1

Test celowo nieudany
    [Documentation]    Pokazuje TEST_MESSAGE dostepne tylko w teardownie.
    Fail    Celowy blad do demonstracji zmiennych teardownu

*** Keywords ***
Zaloguj wynik testu
    Log    Status testu: ${TEST_STATUS}
    Run Keyword If    '${TEST_STATUS}' == 'FAIL'    Zrzut ekranu z komunikatem

Zrzut ekranu z komunikatem
    Log    Test nieudany, powod: ${TEST_MESSAGE}

Otworz plik tymczasowy
    [Documentation]    Keyword z wlasnym teardownem - sprzata sie sam.
    [Teardown]    Log    Sprzatanie po "Otworz plik tymczasowy" wykonane
    Log    Symulacja otwarcia zasobu wewnatrz keyworda
```

```robotframework
# __init__.robot - w katalogu z plikami testowymi definiuje Suite Setup/Teardown
# dla calego katalogu (suite'u nadrzednego), wspolny dla wszystkich plikow .robot w nim.
*** Settings ***
Suite Setup       Log    Wspolny setup dla calego katalogu testow
Suite Teardown    Log    Wspolne sprzatanie po wszystkich plikach w katalogu
```

## 4. Najczestsze bledy
- Proba odczytania ${TEST_STATUS}/${TEST_MESSAGE} w Test Setup lub w ciele
  testu - te zmienne istnieja wylacznie w kontekscie teardownu, wczesniej
  wynik testu jeszcze nie jest znany.
- Zapomnienie, ze __init__.robot dziala TYLKO jako Suite Setup/Teardown
  katalogu (nie mozna w nim definiowac *** Test Cases ***) - proba dodania
  tam testow konczy sie bledem parsowania suite'u.
- Zakladanie, ze skoro Suite Setup sie nie powiodl, testy "po prostu sie nie
  wykonaja" bez sladu w raporcie - w rzeczywistosci wszystkie sa oznaczone
  jako FAIL z komunikatem o bledzie Suite Setup, co warto uwzglednic przy
  analizie duzej liczby czerwonych testow naraz.
- Uzywanie --SkipTeardownOnExit w pipeline'ach CI/CD "na wszelki wypadek" -
  przy przerwaniu joba (np. timeout) zasoby typu przegladarka czy sesja
  API zostaja niezamkniete i moga zaburzac kolejne uruchomienia.

## 5. Ulepszenia z AI
- Poproś Claude/Copilot o przeanalizowanie struktury katalogow testow i
  zaproponowanie, ktore wspolne Setup/Teardown warto przeniesc do
  __init__.robot zamiast powielac je w kazdym pliku .robot.
- Wklej log z nieudanego Suite Setup i poproś AI o wskazanie, czy przyczyna
  lezy w samym Setup, czy to efekt kaskadowy (np. brakujacy plik konfiguracyjny),
  oraz o propozycje bezpieczniejszego teardownu sprzatajacego czesciowo
  zainicjalizowane zasoby.
- Uzyj AI do wygenerowania [Teardown] dla wlasnych keywordow, ktore otwieraja
  zasoby (polaczenia, pliki, sesje) - opisz keyword w jezyku naturalnym, a AI
  zaproponuje symetryczny teardown.
- Poproś AI o przygotowanie listy przypadkow testowych sprawdzajacych
  poprawnosc dziedziczenia Setup/Teardown w hierarchii suite'ow (katalog +
  podkatalog + plik), zeby zweryfikowac kolejnosc wykonania.

## Zadanie na dzis
Stworz strukture katalogow z plikiem __init__.robot definiujacym wspolny
Suite Setup/Teardown oraz jednym plikiem .robot zawierajacym: (1) test
korzystajacy z ${TEST_STATUS} i ${TEST_MESSAGE} w Test Teardown, (2) wlasny
keyword z lokalnym [Teardown], ktory sprzata swoj zasob niezaleznie od
wyniku calego testu.

================================================================================

# Dzien 23 (2026-08-19, sroda) - Biblioteki standardowe runda 2 (mniej oczywiste keywordy Collections, String, OperatingSystem, DateTime)

## 1. Temat dnia
Druga runda bibliotek standardowych - tym razem mniej oczywiste, ale przydatne
keywordy: bezpieczne pobieranie wartosci ze slownika z domyslna wartoscia,
sortowanie i wycinki list, wyrazenia regularne w String, kopiowanie plikow
i legacy Run w OperatingSystem, a takze roznica dwoch dat (Timedelta) w
DateTime.

## 2. Kluczowe zasady i najlepsze praktyki
- Get From Dictionary z parametrem default pozwala uniknac bledu przy
  brakujacym kluczu - czytelniejsze niz reczne sprawdzanie obecnosci klucza
  przez Run Keyword If i wyrazenie Pythona.
- Sort List sortuje liste w miejscu (in-place, modyfikuje oryginalna zmienna
  i zwraca None), a Get Slice From List zwraca nowa liste-fragment bez
  modyfikowania oryginalu - warto pamietac, ktory keyword ma efekt uboczny.
- Get Regexp Matches i Replace String Using Regexp z biblioteki String
  pozwalaja wyciagac/podmieniac fragmenty tekstu wzorcem regex - przydatne
  przy parsowaniu odpowiedzi API czy logow, gdzie Split String nie wystarczy.
- OperatingSystem.Run to przestarzale, blokujace wywolanie polecen
  systemowych bez kontroli nad timeoutem i strumieniami - do uruchamiania
  procesow zewnetrznych w nowych testach lepiej uzyc biblioteki Process
  (Run Process), a OperatingSystem.Run traktowac jako legacy.
- Subtract Date From Date zwraca roznice miedzy datami w sekundach - do
  czytelnej prezentacji uzyj Convert Time z result_format=verbose zamiast
  recznie przeliczac sekundy na godziny/minuty.

## 3. Przykladowy kod
```robotframework
*** Settings ***
Library    Collections
Library    String
Library    OperatingSystem
Library    DateTime

*** Test Cases ***
Bezpieczny dostep do slownika i operacje na liscie
    [Documentation]    Get From Dictionary z domyslna wartoscia, Sort List i Get Slice From List.
    &{uzytkownik}=    Create Dictionary    imie=Anna    wiek=28
    ${miasto}=    Get From Dictionary    ${uzytkownik}    miasto    default=Nieznane
    Should Be Equal    ${miasto}    Nieznane

    @{liczby}=    Create List    5    1    4    2    3
    Sort List    ${liczby}
    Should Be Equal As Strings    ${liczby}    ['1', '2', '3', '4', '5']

    @{fragment}=    Get Slice From List    ${liczby}    1    3
    Log    Wycinek listy: ${fragment}

Regex w Stringu i operacje na plikach
    [Documentation]    Wyrazenia regularne oraz kopiowanie plikow przez OperatingSystem.
    ${log}=    Set Variable    Blad 404: strona nie znaleziona, kod=404
    @{kody}=    Get Regexp Matches    ${log}    kod=(\\d+)    1
    Should Be Equal    ${kody}[0]    404

    Create File    ${OUTPUT_DIR}/dane.txt    Zawartosc pliku zrodlowego
    Copy File    ${OUTPUT_DIR}/dane.txt    ${OUTPUT_DIR}/kopia_dane.txt
    File Should Exist    ${OUTPUT_DIR}/kopia_dane.txt

Roznica dat i czytelny czas trwania
    [Documentation]    Subtract Date From Date i Convert Time do prezentacji roznicy.
    ${start}=    Set Variable    2026-08-19 08:00:00
    ${koniec}=    Set Variable    2026-08-19 10:30:00
    ${roznica_sek}=    Subtract Date From Date    ${koniec}    ${start}
    ${roznica_czytelna}=    Convert Time    ${roznica_sek}    result_format=verbose
    Log    Czas trwania: ${roznica_czytelna}
```

## 4. Najczestsze bledy
- Uzywanie Get From Dictionary bez default tam, gdzie klucz moze nie
  istniec - test pada bledem zamiast swiadomej obslugi brakujacej wartosci.
- Zakladanie, ze Sort List zwraca nowa posortowana liste - w rzeczywistosci
  sortuje w miejscu i zwraca None, wiec przypisanie wyniku do nowej
  zmiennej daje ${None} zamiast posortowanej listy.
- Uzywanie OperatingSystem.Run do uruchamiania dlugotrwalych procesow (np.
  serwera testowego) - brak kontroli nad timeoutem/wyjsciem procesu
  prowadzi do zawieszania sie calego przebiegu testow.
- Parsowanie zlozonych struktur tekstu recznymi wywolaniami Split String
  zamiast Get Regexp Matches - kod staje sie krucha seria warunkow zamiast
  jednego czytelnego wzorca regex.
- Podawanie do Subtract Date From Date dat w roznych formatach lub strefach
  czasowych bez ujednolicenia - roznica wychodzi bledna, a blad latwo
  przeoczyc, bo test i tak formalnie "przechodzi".

## 5. Ulepszenia z AI
- Wklej fragment logu/tekstu (np. odpowiedz API) i poproś Claude/Copilot o
  zaproponowanie wzorca regex do Get Regexp Matches/Replace String Using
  Regexp wyciagajacego konkretne dane.
- Poproś AI o przejrzenie istniejacych testow pod katem miejsc, gdzie Get
  From Dictionary powinien miec parametr default, zeby test nie padal na
  brakujacym, opcjonalnym polu.
- Uzyj AI do zamiany starych wywolan OperatingSystem.Run na odpowiednik z
  biblioteki Process (Run Process) wraz z sensownym timeoutem.
- Poproś AI o wygenerowanie zestawu przypadkow testowych na roznice dat
  (Subtract Date From Date) obejmujacych rozne strefy czasowe i formaty
  wejsciowe.

## Zadanie na dzis
Napisz test, ktory: (1) pobierze wartosc ze slownika przez Get From
Dictionary z domyslna wartoscia dla nieistniejacego klucza, (2) posortuje
liste liczb przez Sort List i pobierze jej fragment przez Get Slice From
List, (3) wyciagnie kod bledu z przykladowego tekstu logu przez Get Regexp
Matches, (4) obliczy i wypisze czytelna roznice miedzy dwiema datami przez
Subtract Date From Date i Convert Time.

================================================================================
# Dzien 24 (2026-08-22, sobota) - Testy webowe SeleniumLibrary/Browser Library runda 2 (iframe, upload plikow, mockowanie sieci, wiele kontekstow)

## 1. Temat dnia
Druga runda testow webowych - tym razem mniej oczywiste, ale czesto potrzebne
mechanizmy Browser Library (Playwright): praca z elementami wewnatrz iframe,
upload plikow, oczekiwanie na konkretne zadania sieciowe (Wait For Response)
zamiast Sleep, izolacja sesji przez wiele kontekstow oraz Evaluate JavaScript
jako "wyjscie awaryjne".

## 2. Kluczowe zasady i najlepsze praktyki
- Selektor elementu wewnatrz iframe buduje sie laczac selektor ramki i
  elementu operatorem `>>>` (np. `iframe#platnosc >>> css=#numer-karty`) -
  nie trzeba recznego przelaczania kontekstu jak w Selenium (Switch To Frame).
- Do testowania uploadu plikow uzywaj keywordu `Upload File` wskazujac
  sciezke lokalnego pliku - dziala nawet gdy input[type=file] jest ukryty
  (display:none), bo Playwright operuje na API przegladarki, nie na
  widocznosci elementu.
- `Promise To    Wait For Response` trzeba zarejestrowac PRZED akcja, ktora
  wywoluje zadanie sieciowe, a dopiero potem uzyc `Wait For` na zwroconym
  promise - pozwala to poczekac na konkretne API zamiast zgadywac czas Sleep.
- `New Context` + `New Page` izoluje ciasteczka/local storage miedzy
  scenariuszami (np. rozni uzytkownicy) w jednym pliku testowym, bez potrzeby
  restartowania calej przegladarki.
- `Evaluate JavaScript` daje dostep do DOM/API przegladarki, gdy nie ma
  gotowego keyworda (np. odczyt wartosci z localStorage) - traktuj to jako
  wyjscie awaryjne, nie podstawowy sposob interakcji z aplikacja.

## 3. Przykladowy kod
```robotframework
*** Settings ***
Library    Browser

Suite Setup       New Browser    chromium    headless=true
Suite Teardown    Close Browser
Test Setup        New Context    New Page    https://example.com/dashboard

*** Test Cases ***
Platnosc przez formularz osadzony w iframe
    [Documentation]    Wypelnienie pola numeru karty osadzonego w iframe.
    [Tags]    web    iframe
    Fill Text    iframe#platnosc >>> css=#numer-karty    4111111111111111
    Click    iframe#platnosc >>> css=#przycisk-zaplac
    Get Text    css=.status-platnosci    ==    Platnosc zaakceptowana

Upload zalacznika do formularza
    [Documentation]    Upload pliku lokalnego przez ukryty input[type=file].
    [Tags]    web    upload
    Upload File    css=input[type="file"]    ${CURDIR}/testdata/cv.pdf
    Click    css=#wyslij-formularz
    Get Text    css=.potwierdzenie    contains    Plik zostal przeslany

Oczekiwanie na odpowiedz API po kliknieciu
    [Documentation]    Wait For Response zamiast Sleep po akcji wywolujacej zapytanie.
    [Tags]    web    network
    ${promise}=    Promise To    Wait For Response    matcher=**/api/zamowienia**
    Click    css=#zloz-zamowienie
    ${odpowiedz}=    Wait For    ${promise}
    Should Be Equal As Integers    ${odpowiedz}[status]    201

Dwaj uzytkownicy w niezaleznych kontekstach
    [Documentation]    New Context izoluje sesje/ciasteczka miedzy "uzytkownikami".
    [Tags]    web    multi-context
    New Context
    New Page    https://example.com/login
    Fill Text    css=#login    admin

    New Context
    New Page    https://example.com/login
    Fill Text    css=#login    gosc
```

## 4. Najczestsze bledy
- Probowanie przelaczenia kontekstu do iframe osobnym keywordem (jak
  Selenium Switch To Frame) - Browser Library nie ma takiego kroku, selektor
  iframe laczy sie bezposrednio operatorem `>>>` z selektorem elementu.
- Symulowanie uploadu przez reczne "przeciaganie" pliku zamiast Upload File -
  niepotrzebnie komplikuje test i bywa niestabilne na roznych systemach.
- Rejestrowanie `Promise To    Wait For Response` PO kliknieciu wywoluja-
  cym zadanie - zadanie moze zdazyc wystartowac i zakonczyc sie, zanim
  keyword zacznie nasluchiwac, przez co Wait For nigdy nie zlapie odpowiedzi.
- Nadmierne poleganie na Evaluate JavaScript zamiast wbudowanych keywordow -
  test przestaje byc czytelny dla osob nieznajacych JS i traci zalety
  warstwy abstrakcji Browser Library.
- Brak New Context per niezalezny scenariusz uzytkownika - testy "dziedzicza"
  ciasteczka/sesje po sobie i wynik zaczyna zalezec od kolejnosci uruchomienia.

## 5. Ulepszenia z AI
- Wklej fragment HTML z zagniezdzonym iframe do AI i poproś o zlozenie
  poprawnego selektora z operatorem `>>>` do konkretnego pola formularza.
- Poproś AI o zaproponowanie matchera URL (wzorzec glob/regex) do Wait For
  Response na podstawie przykladowego adresu zadania z zakladki Network w
  devtools przegladarki.
- Uzyj AI do wygenerowania scenariuszy testowych na upload plikow (zle
  rozszerzenie, za duzy plik, pusty plik) na podstawie jednego dzialajacego
  testu.
- Poproś Claude/Copilot o przeanalizowanie loga bledu Browser Library (np.
  timeout na Wait For Response) i zaproponowanie przyczyny (zly matcher,
  akcja wywolana przed rejestracja Promise To).
- Poproś AI o wygenerowanie Resource file z keywordami wysokiego poziomu
  ("Zaloguj jako administrator", "Zaloguj jako gosc") opartymi na New
  Context, zeby ukryc szczegoly izolacji sesji przed testami.

## Zadanie na dzis
Napisz test Browser Library, ktory: (1) otworzy strone z formularzem
zawierajacym input[type=file] i wykona Upload File dla lokalnego pliku,
(2) zarejestruje Promise To Wait For Response PRZED kliknieciem przycisku
wysylajacego formularz i sprawdzi kod statusu odpowiedzi API, (3) utworzy
dwa niezalezne konteksty (New Context) symulujace dwoch roznych uzytkownikow
zalogowanych jednoczesnie w jednym uruchomieniu testu.

================================================================================
# Dzien 25 (2026-08-23, niedziela) - RequestsLibrary runda 2 (autoryzacja, wspolne naglowki, PUT/PATCH/DELETE, ponawianie zadan)

## 1. Temat dnia
Druga runda RequestsLibrary - tym razem tematy pomijane przy pierwszym podejsciu:
przekazywanie wspolnych naglowkow (w tym tokenu Bearer) do sesji, aktualizacja i
usuwanie zasobow (PUT/PATCH/DELETE On Session), parametry zapytania (params) oraz
odporne na chwilowa niedostepnosc API ponawianie zadan przez Wait Until Keyword
Succeeds w polaczeniu z TRY/EXCEPT.

## 2. Kluczowe zasady i najlepsze praktyki
- Naglowki wspolne dla calej sesji (np. token Bearer, Content-Type) podawaj
  raz w `Create Session` przez parametr `headers` - unikniesz powielania tego
  samego slownika naglowkow w kazdym wywolaniu GET/POST/PUT/DELETE On Session.
- Do czesciowej aktualizacji zasobu uzywaj `PATCH On Session` (tylko wybrane
  pola), a do pelnej podmiany calego zasobu `PUT On Session` (wszystkie pola) -
  mieszanie tych dwoch podejsc bez zastanowienia prowadzi do przypadkowego
  nadpisania pol, ktorych test nie chcial zmieniac.
- Parametry zapytania (query string) przekazuj przez argument `params`
  (slownik), a nie recznie sklejany string w URL - RF/requests poprawnie
  zakoduje wtedy znaki specjalne.
- Do zadan, ktore moga chwilowo zawiesc (np. API jeszcze "rozgrzewa sie" po
  restarcie), stosuj `Wait Until Keyword Succeeds` z rozsadna liczba prob i
  odstepem czasu zamiast recznej petli FOR ze Sleep.
- Blad polaczenia (np. `ConnectionError`) lap przez `TRY`/`EXCEPT` i zglaszaj
  czytelny komunikat diagnostyczny (`Fail`) zamiast pozwalac testowi
  zakonczyc sie surowym tracebackiem Pythona.

## 3. Przykladowy kod
```robotframework
*** Settings ***
Library           RequestsLibrary
Library           Collections

*** Variables ***
${BASE_URL}       https://jsonplaceholder.typicode.com
${API_TOKEN}      %{API_TOKEN=demo-token-123}

*** Test Cases ***
Sesja ze wspolnymi naglowkami i tokenem
    [Documentation]    Tworzy sesje z naglowkiem Authorization wspolnym dla wszystkich zadan.
    [Tags]    api
    &{naglowki}=    Create Dictionary
    ...    Authorization=Bearer ${API_TOKEN}
    ...    Content-Type=application/json
    Create Session    api    ${BASE_URL}    headers=${naglowki}    timeout=10
    ${response}=    GET On Session    api    /posts/1    expected_status=200
    Should Be Equal    ${response.request.headers}[Authorization]    Bearer ${API_TOKEN}

Czesciowa aktualizacja zasobu (PATCH) i pelna podmiana (PUT)
    [Documentation]    Roznica miedzy PATCH (czesciowa zmiana) a PUT (pelna podmiana).
    [Tags]    api
    Create Session    api    ${BASE_URL}    timeout=10
    &{poprawka}=    Create Dictionary    title=Zaktualizowany tytul
    ${response_patch}=    PATCH On Session    api    /posts/1    json=${poprawka}    expected_status=200
    Should Be Equal    ${response_patch.json()}[title]    Zaktualizowany tytul

    &{pelny_obiekt}=    Create Dictionary    id=1    title=Nowy    body=Nowa tresc    userId=1
    ${response_put}=    PUT On Session    api    /posts/1    json=${pelny_obiekt}    expected_status=200
    Dictionary Should Contain Key    ${response_put.json()}    body

Parametry zapytania i ponawianie niestabilnego zadania
    [Documentation]    Query params przez "params" oraz retry przez Wait Until Keyword Succeeds.
    [Tags]    api
    Create Session    api    ${BASE_URL}    timeout=10
    &{filtr}=    Create Dictionary    userId=1
    ${response}=    GET On Session    api    /posts    params=${filtr}    expected_status=200
    Should Not Be Empty    ${response.json()}

    Wait Until Keyword Succeeds    3x    2s
    ...    GET On Session    api    /posts/1    expected_status=200

Usuniecie zasobu z obsluga bledu polaczenia
    [Documentation]    DELETE On Session wraz z TRY/EXCEPT na wypadek problemu z siecia.
    [Tags]    api
    Create Session    api    ${BASE_URL}    timeout=10
    TRY
        ${response}=    DELETE On Session    api    /posts/1    expected_status=200
    EXCEPT    AS    ${blad}
        Fail    Usuniecie zasobu nie powiodlo sie: ${blad}
    END
    Should Be Equal As Integers    ${response.status_code}    200
```

## 4. Najczestsze bledy
- Powtarzanie tego samego slownika naglowkow (np. tokenu) w kazdym wywolaniu
  GET/POST/PUT/DELETE On Session zamiast przekazac go raz do Create Session -
  wieksze ryzyko literowki i trudniejsza zmiana tokenu w calym suite.
- Uzywanie PUT tam, gdzie potrzebna jest tylko czesciowa aktualizacja - API,
  ktore nie dostanie wszystkich wymaganych pol w PUT, moze wyzerowac pozostale
  wartosci zasobu.
- Recznie sklejany URL z parametrami (`/posts?userId=1&_limit=5` jako string)
  zamiast argumentu `params` - latwo o bledy kodowania znakow specjalnych
  (spacje, znaki diakrytyczne) i trudniejsza czytelnosc testu.
- Petla FOR ze Sleep imitujaca ponawianie zadania zamiast wbudowanego Wait
  Until Keyword Succeeds - wiecej kodu do utrzymania i brak jasnego limitu
  prob/czasu w jednym miejscu.
- Brak obslugi wyjatku polaczenia (np. gdy API jest chwilowo niedostepne) -
  test konczy sie surowym tracebackiem Pythona zamiast czytelnym komunikatem
  ulatwiajacym diagnoze w raporcie.

## 5. Ulepszenia z AI
- Wklej dokumentacje API (np. fragment specyfikacji OpenAPI dotyczacy PATCH
  vs PUT) i poproś Claude/Copilot o wygenerowanie osobnych testow dla obu
  operacji z poprawnymi payloadami.
- Poproś AI o przeglad istniejacych testow RequestsLibrary pod katem
  duplikowania naglowkow autoryzacji i przeniesienie ich do wspolnego
  Create Session (refaktoryzacja).
- Uzyj AI do zaproponowania rozsadnych parametrow Wait Until Keyword Succeeds
  (liczba prob, odstep czasu) na podstawie opisanego zachowania API (np. czas
  "rozgrzewania" po restarcie).
- Poproś AI o wygenerowanie bloku TRY/EXCEPT z czytelnym komunikatem bledu na
  podstawie typowych wyjatkow biblioteki requests (ConnectionError, Timeout).
- Wklej przykladowy URL z recznie sklejanymi parametrami i poproś AI o
  przepisanie go na wywolanie z argumentem `params` jako slownik.

## Zadanie na dzis
Napisz suite RequestsLibrary, ktory: (1) utworzy sesje ze wspolnym naglowkiem
Authorization przekazanym raz w Create Session, (2) wykona test PATCH
aktualizujacy jedno pole zasobu i zweryfikuje odpowiedz, (3) wykona test GET
z parametrami zapytania przekazanymi przez argument params, (4) opakuje jedno
z zadan w Wait Until Keyword Succeeds oraz w blok TRY/EXCEPT z czytelnym
komunikatem bledu na wypadek problemu z polaczeniem.

================================================================================


# Dzień 26 (2026-08-24, poniedziałek) - Dane testowe i Data-Driven Testing runda 2 (Test Template na poziomie suite'u, argumenty słownikowe, biblioteka DataDriver, nazwy wierszy)

## 1. Temat dnia
Druga runda tematu Data-Driven Testing - tym razem zagadnienia pominięte przy
pierwszym podejściu: globalny `Test Template` ustawiany raz dla całego suite'u
(zamiast `[Template]` w każdym Test Case osobno), przekazywanie argumentów do
szablonu jako słownik (`&{...}`) zamiast pozycyjnych wartości, zewnętrzna
biblioteka `DataDriver` do wczytywania danych bezpośrednio z pliku CSV/XLSX bez
ręcznego pisania tabeli w `*** Test Cases ***`, a także sterowanie nazwami
wierszy i kontynuacją wykonania mimo błędu w pojedynczym przypadku.

## 2. Kluczowe zasady i najlepsze praktyki
- `Test Template` w sekcji `*** Settings ***` ustawia domyślny szablon dla
  wszystkich Test Case'ów w danym pliku - poszczególne testy mogą go nadpisać
  własnym `[Template]`, ale gdy cały suite testuje jedną regułę biznesową,
  ustawienie go globalnie skraca kod i eliminuje powtórzenia.
- Argumenty do keywordu szablonowego można przekazywać jako słownik
  (`&{dane}=    Create Dictionary    ...` + rozpakowanie `&{dane}` w wierszu
  Test Case) - ułatwia to czytanie danych wprost z pliku JSON/YAML bez
  ręcznego rozbijania na pozycyjne kolumny.
- Do dużych zestawów danych (dziesiątki/setki wierszy) zamiast ręcznie pisanej
  tabeli warto użyć biblioteki `DataDriver` - wczytuje ona dane z pliku CSV
  lub XLSX i automatycznie generuje osobny Test Case dla każdego wiersza,
  z nazwą testu pobraną z kolumny `*** Test Cases ***`/`Name`.
- Nazwę każdego wygenerowanego przypadku (czy to w ręcznej tabeli, czy przez
  DataDriver) projektuj tak, by od razu było wiadomo, jaki scenariusz biznesowy
  reprezentuje - ułatwia to filtrowanie po nazwie (`--test "*puste haslo*"`)
  bez otwierania kodu testu.
- Domyślnie każdy wiersz w Template jest niezależnym testem - błąd w jednym
  nie przerywa pozostałych, więc przy analizie wyniku suite'u sprawdzaj
  wszystkie przypadki w report.html, a nie tylko pierwszy napotkany błąd.

## 3. Przykładowy kod
Globalny `Test Template` + argumenty jako słownik:
```robotframework
*** Settings ***
Documentation     DDT z globalnym Test Template i argumentami slownikowymi.
Library           Collections
Test Template     Sprawdz walidacje rejestracji

*** Test Cases ***                    DANE
Poprawna rejestracja                  ${DANE_1}
Rejestracja z pustym emailem          ${DANE_2}
Rejestracja ze zbyt krotkim haslem    ${DANE_3}

*** Variables ***
&{DANE_1}         email=jan@firma.pl    haslo=BardzoTajneHaslo1    oczekiwany=SUKCES
&{DANE_2}         email=${EMPTY}        haslo=BardzoTajneHaslo1    oczekiwany=BLAD
&{DANE_3}         email=jan@firma.pl    haslo=abc                  oczekiwany=BLAD

*** Keywords ***
Sprawdz walidacje rejestracji
    [Arguments]    &{dane}
    ${wynik}=    Zarejestruj    email=${dane}[email]    haslo=${dane}[haslo]
    Should Be Equal    ${wynik}    ${dane}[oczekiwany]

Zarejestruj
    [Arguments]    ${email}    ${haslo}
    IF    "${email}" == "${EMPTY}" or len($haslo) < 8
        RETURN    BLAD
    END
    RETURN    SUKCES
```

Wczytywanie danych z pliku CSV przez bibliotekę DataDriver (wymaga
`pip install robotframework-datadriver`):
```robotframework
*** Settings ***
Library           DataDriver    file=dane_rejestracji.csv    encoding=UTF-8
Test Template     Sprawdz walidacje rejestracji

*** Test Cases ***
Szablon rejestracji    DEFAULT

*** Keywords ***
Sprawdz walidacje rejestracji
    [Arguments]    ${email}    ${haslo}    ${oczekiwany}
    ${wynik}=    Zarejestruj    ${email}    ${haslo}
    Should Be Equal    ${wynik}    ${oczekiwany}
```
Przykładowy `dane_rejestracji.csv`:
```
*** Test Cases ***,email,haslo,oczekiwany
Poprawna rejestracja,jan@firma.pl,BardzoTajneHaslo1,SUKCES
Pusty email,,BardzoTajneHaslo1,BLAD
Za krotkie haslo,jan@firma.pl,abc,BLAD
```

## 4. Najczęstsze błędy
- Kopiowanie identycznego `[Template]` do każdego Test Case w pliku zamiast
  jednorazowego ustawienia `Test Template` w `*** Settings ***`, gdy cały
  suite testuje jedną regułę - niepotrzebne powielanie kodu.
- Rozpakowywanie słownika argumentów bez wskazania kluczy w keywordzie
  (`${dane}[klucz]`) - użycie samych pozycji zamiast nazwanych pól utrudnia
  czytanie testu i psuje się przy zmianie kolejności pól w danych.
- Instalacja `DataDriver` bez ustawienia poprawnego `encoding` dla plików CSV
  zawierających polskie znaki - dane z ogonkami (ą, ę, ł) trafiają do testu
  jako zniekształcone znaki, a błąd bywa mylnie diagnozowany jako problem z
  logiką testu.
- Nazwa pierwszego Test Case w pliku z DataDriver ("Szablon rejestracji") jest
  traktowana jako placeholder, ale bywa mylona z realną nazwą przypadku - w
  report.html i tak liczą się nazwy wygenerowane z kolumny danych.
- Brak nagłówka zgodnego z nazwami argumentów keywordu w pliku CSV/XLSX -
  DataDriver dopasowuje kolumny po nazwie, więc literówka w nagłówku pliku
  danych powoduje brak przekazania wartości do testu (pusty argument) zamiast
  czytelnego błędu w momencie uruchomienia.

## 5. Ulepszenia z AI
- Wklej przykładowy wiersz danych i opis reguły biznesowej, a poproś Claude/
  Copilot o wygenerowanie kompletnego pliku CSV z kombinacjami przypadków
  brzegowych gotowego do podpięcia pod DataDriver.
- Poproś AI o przepisanie istniejącej ręcznej tabeli `[Template]` na wariant
  z globalnym `Test Template` i argumentami słownikowymi (`&{...}`), gdy
  liczba kolumn w tabeli zaczyna przeszkadzać w czytelności.
- Użyj AI do wygenerowania nazw przypadków testowych (kolumna `Test Cases`/
  `Name` w CSV) na podstawie samych wartości danych, tak by od razu opisywały
  scenariusz biznesowy zamiast "Wiersz 1", "Wiersz 2".
- Poproś AI o przegląd pliku danych pod kątem duplikatów i brakujących
  kombinacji (macierz parametrów) przed podłączeniem go pod DataDriver.
- Wklej log błędu z niezgodną liczbą/nazwą argumentów w wierszu Template i
  poproś AI o wskazanie, w którym wierszu danych brakuje kolumny lub jest
  literówka w nagłówku.

## Zadanie na dziś
Przekształć istniejący test z ręczną tabelą `[Template]` (np. z Dnia 8) tak,
aby: (1) zamiast `[Template]` w Test Case użyć globalnego `Test Template` w
sekcji `*** Settings ***`, (2) argumenty do keywordu szablonowego przekazać
jako słownik `&{dane}` odczytywany po nazwanych kluczach, (3) dodać co
najmniej jeden przypadek z pustą wartością pola i sprawdzić, że nazwa tego
wiersza w report.html jasno opisuje testowany scenariusz.

================================================================================

================================================================================
# Dzień 27 (2026-08-25, wtorek) - Tagowanie i selektywne uruchamianie testów runda 2 (Set Tags/Remove Tags, Keyword Tags, wzorce z NOT, rebot)

## 1. Temat dnia
Druga runda tematu tagowania - tym razem mechanizmy pomijane przy pierwszym
podejściu: dynamiczne nadawanie i usuwanie tagów w trakcie wykonania testu
(`Set Tags` / `Remove Tags`), tagowanie na poziomie całego pliku Keywords
(`Keyword Tags`), bardziej złożone wzorce filtrowania z operatorem `NOT` i
maskami (`*`), różnica między `--skip` a `--exclude`, oraz narzędzie `rebot`
pozwalające przefiltrować i przetworzyć wyniki z już istniejącego `output.xml`
bez ponownego uruchamiania testów.

## 2. Kluczowe zasady i najlepsze praktyki
- `Set Tags` i `Remove Tags` (BuiltIn) pozwalają nadać lub usunąć tag
  bieżącego testu w trakcie jego wykonania - przydatne np. do oznaczania
  testu jako "flaky" po nieudanej pierwszej próbie i udanym powtórzeniu, albo
  do oznaczenia "slow" na podstawie realnego czasu wykonania.
- `Keyword Tags` w sekcji `*** Settings ***` pliku resource nadaje domyślny
  tag wszystkim keywordom zdefiniowanym w tym pliku (widoczny w log.html przy
  wywołaniach) - ułatwia odróżnienie, z którego resource'a pochodzi dany krok,
  bez ręcznego dopisywania `[Tags]` przy każdym keywordzie.
- Wzorce filtrowania obsługują `NOT` (`smokeANDNOTslow` - testy z tagiem
  smoke, ale bez slow) oraz maski `*`/`?` (`--include regression*`) - warto
  je znać zamiast wielokrotnego łączenia osobnych `--exclude`.
- `--skip` różni się od `--exclude`: pominięty tak testem `--skip` nadal
  pojawia się w report.html/log.html ze statusem SKIP i wpływa na statystyki,
  a wykluczony przez `--exclude` w ogóle nie jest uruchamiany ani raportowany.
- `rebot` pozwala wygenerować nowy log.html/report.html (np. z innym zestawem
  `--include`/`--exclude`, scaleniem kilku przebiegów) z istniejącego
  `output.xml` - nie trzeba ponownie odpalać testów, żeby zobaczyć inny
  przekrój tych samych wyników.

## 3. Przykładowy kod
Dynamiczne tagowanie na podstawie wyniku wykonania:
```robotframework
*** Settings ***
Documentation     Dynamiczne tagowanie testu jako "flaky" po powtorce.
Library           Collections

*** Test Cases ***
Logowanie przez API z ponowna proba
    [Tags]    api    login
    ${status}=    Sprobuj zalogowac z powtorka

Sprobuj zalogowac z powtorka
    [Documentation]    Pomocnicza sekwencja - w realnym tescie to bylby
    ...                osobny keyword wywolywany z Test Case.
    Log    Symulacja pierwszej nieudanej proby logowania
    Run Keyword And Ignore Error    Fail    Chwilowy blad sieci
    Set Tags    flaky
    Log    Symulacja udanej drugiej proby logowania
```

Tag domyślny dla wszystkich keywordów w pliku resource:
```robotframework
*** Settings ***
Documentation    Keywordy do logowania - wszystkie oznaczone tagiem "auth".
Keyword Tags     auth

*** Keywords ***
Zaloguj poprawnym uzytkownikiem
    Log    Symulacja logowania

Wyloguj biezacego uzytkownika
    Log    Symulacja wylogowania
```

Filtrowanie z operatorem NOT i maską oraz różnica `--skip` vs `--exclude`:
```bash
# smoke, ale bez testow oznaczonych jako wolne
robot --include smokeANDNOTslow tests/

# wszystkie tagi zaczynajace sie od "regression"
robot --include regression* tests/

# testy sie uruchamiaja, ale te z tagiem "wip" pokazuja sie jako SKIP
robot --skip wip tests/
```

Ponowne przetworzenie wyniku bez ponownego uruchamiania testów (`rebot`):
```bash
# najpierw pelny przebieg, zapisuje output.xml
robot --outputdir results tests/

# potem osobny raport tylko dla podzbioru tagow, z tego samego output.xml
rebot --include smoke --outputdir results/smoke-only results/output.xml
```

## 4. Najczęstsze błędy
- Wywołanie `Set Tags` wewnątrz `[Teardown]` z oczekiwaniem, że tag pojawi
  się "przed" testem w raporcie na żywo - tag faktycznie trafia do wyniku
  testu, ale kolejność w kodzie i tak musi być logiczna, bo tag ustawiony w
  teardownie nie wpływa już na warunki `--include`/`--exclude` przy tym samym
  uruchomieniu (te są rozstrzygane przed startem testu).
- Mylenie `--skip` z `--exclude` - `--skip` nadal generuje wpis testu w
  statystykach (jako SKIP), więc "ukrycie" testu w ten sposób nie zmniejsza
  liczby testów widocznych w report.html tak, jak zrobiłby to `--exclude`.
- Zapominanie, że `rebot` operuje wyłącznie na tym, co już jest w
  `output.xml` - test wykluczony przez `--exclude` przy oryginalnym
  uruchomieniu `robot` nie da się "odzyskać" żadnym filtrem w `rebot`, bo
  nigdy nie został wykonany ani zapisany.
- Ustawienie `Keyword Tags` w resource file zawierającym keywordy o różnym
  przeznaczeniu (np. i logowanie, i operacje na bazie danych) - wszystkie
  dostają ten sam tag, co zaciemnia log.html zamiast go porządkować.
- Nieprawidłowa składnia wzorca z NOT (spacje albo małe/wielkie litery
  niezgodne z resztą projektu) - `smoke AND NOT slow` z odstępami nie
  zadziała tak samo jak `smokeANDNOTslow` bez spacji w wierszu poleceń.

## 5. Ulepszenia z AI
- Wklej listę tagów używanych w projekcie i poproś Claude/Copilot o
  zaproponowanie reguł dynamicznego tagowania (`Set Tags`) na podstawie
  typowych warunków w kodzie testów (np. "otaguj jako flaky, gdy pierwsza
  próba się nie powiodła, a druga tak").
- Poproś AI o wygenerowanie polecenia `rebot` z konkretną kombinacją
  `--include`/`--exclude`/`--merge` na podstawie opisu "chcę osobny raport
  tylko dla testów API oznaczonych jako critical, bez testów wolnych".
- Wklej plik resource i zapytaj AI, czy `Keyword Tags` na poziomie pliku ma
  sens, czy lepiej rozbić plik na mniejsze resource'y o spójnym zakresie
  odpowiedzialności keywordów.
- Użyj AI do przeglądu wyrażeń filtrujących z operatorami AND/OR/NOT pod
  kątem literówek i niejednoznaczności przed wklejeniem ich do pipeline'u
  CI/CD.
- Wklej fragment log.html/output.xml z testami oznaczonymi `Set Tags flaky`
  i poproś AI o podsumowanie, które obszary aplikacji generują najwięcej
  niestabilnych testów.

## Zadanie na dziś
Rozbuduj dowolny istniejący test (np. z Dnia 9) tak, aby: (1) w keywordzie
pomocniczym wywołał `Set Tags` z tagiem `flaky` w scenariuszu symulującym
nieudaną pierwszą próbę i udaną drugą, (2) uruchom cały suite z `--outputdir`
wskazującym osobny katalog na wynik, (3) bez ponownego uruchamiania testów
użyj `rebot --include flaky` na zapisanym `output.xml`, aby wygenerować
osobny raport tylko dla testów, które faktycznie dostały ten tag w trakcie
wykonania.

================================================================================
# Dzień 28 (2026-08-26, środa) - Resource files i modularizacja testów runda 2 (zagnieżdżone importy, __init__.robot, konflikty nazw, libdoc)

## 1. Temat dnia
Druga runda tematu Resource files - tym razem sprawy pomijane przy pierwszym
podejściu: zagnieżdżone importy (resource importujący inny resource), plik
`__init__.robot` do ustawień na poziomie całego katalogu testów, rozwiązywanie
konfliktów nazw keywordów pochodzących z różnych resource'ów (`Library Search
Order` / prefiksowanie źródłem), oddzielanie zmiennych do własnego pliku oraz
generowanie dokumentacji modułów poleceniem `libdoc`.

## 2. Kluczowe zasady i najlepsze praktyki
- Resource może importować inny resource - buduj hierarchię od ogółu do
  szczegółu (np. `common.resource` z podstawowymi keywordami, a
  `checkout.resource` importuje `common.resource` i dokłada logikę specyficzną
  dla koszyka), ale pamiętaj, że RF wykryje i zgłosi błąd przy cyklu importów.
- Plik `__init__.robot` umieszczony w katalogu z testami definiuje
  `Suite Setup`/`Suite Teardown`/`Force Tags`/importy wspólne dla WSZYSTKICH
  plików `.robot` w tym katalogu i podkatalogach - przydatne np. do jednego
  logowania przed całym folderem testów regresji zamiast powtarzania go w
  każdym pliku.
- Gdy dwa zaimportowane resource'y mają keyword o tej samej nazwie, RF zgłasza
  błąd niejednoznaczności przy jego użyciu - rozwiąż to przez `Library Search
  Order` w BuiltIn albo (lepiej) przez zmianę nazwy jednego z keywordów, żeby
  uniknąć kolizji na starcie.
- Zmienne wspólne dla wielu resource'ów trzymaj w osobnym pliku `.resource`
  z samą sekcją `*** Variables ***` (albo w pliku `.py`/`.yaml` importowanym
  przez `Variables`), a nie rozrzucone po kilku plikach z keywordami - jedno
  źródło prawdy ułatwia zmianę środowiska (np. adresu URL) w jednym miejscu.
- Dokumentuj resource'y tak, by dało się wygenerować z nich czytelną
  dokumentację poleceniem `libdoc` - każdy zespół korzystający z modułu
  powinien móc sprawdzić dostępne keywordy bez czytania kodu źródłowego.

## 3. Przykład kodu (.robot)
Plik `resources/common.resource` (baza, importowana przez inne resource'y):
```robotframework
*** Settings ***
Documentation     Wspolne keywordy bazowe - logowanie i nawigacja.
Library           SeleniumLibrary

*** Variables ***
${BASE_URL}       https://example-shop.test

*** Keywords ***
Otworz Strone Glowna
    Open Browser    ${BASE_URL}    headlesschrome
    Maximize Browser Window
```

Plik `resources/checkout.resource` (importuje `common.resource` i dokłada
własną logikę - hierarchia od ogółu do szczegółu):
```robotframework
*** Settings ***
Documentation     Keywordy koszyka i platnosci - buduja na common.resource.
Resource          ${CURDIR}/common.resource

*** Keywords ***
Dodaj Produkt Do Koszyka
    [Arguments]    ${nazwa_produktu}
    Otworz Strone Glowna
    Log    Dodawanie produktu do koszyka: ${nazwa_produktu}

Przejdz Do Podsumowania Zamowienia
    Log    Przejscie do podsumowania zamowienia
```

Plik `tests/__init__.robot` (ustawienia wspólne dla całego katalogu `tests/`):
```robotframework
*** Settings ***
Documentation     Wspolny Suite Setup/Teardown dla wszystkich testow w katalogu.
Resource          ${CURDIR}/../resources/common.resource
Suite Setup       Log    Start calego pakietu testow zakupowych
Suite Teardown    Log    Koniec calego pakietu testow zakupowych
Force Tags        checkout_suite
```

Sprawdzenie i wygenerowanie dokumentacji resource'a poleceniem `libdoc`:
```bash
# generuje czytelny plik HTML z lista keywordow, argumentow i dokumentacji
python -m robot.libdoc resources/checkout.resource docs/checkout_keywords.html
```

## 4. Najczęstsze błędy
- Import cykliczny między resource'ami (A importuje B, a B z powrotem A) -
  Robot Framework zgłasza błąd parsowania przy starcie i żaden test w ogóle
  się nie uruchomi, więc łatwo pomylić to z błędem w samym teście.
- Nazwanie pliku z ustawieniami wspólnymi inaczej niż `__init__.robot` (np.
  `setup.robot`) w katalogu testów - RF wtedy w ogóle go nie rozpozna jako
  plik inicjujący pakiet i `Suite Setup` z niego nigdy się nie wykona.
- Dwa resource'y z keywordem o identycznej nazwie zaimportowane w jednym
  pliku testowym bez ustawienia `Library Search Order` ani zmiany nazwy -
  test kończy się błędem niejednoznaczności dopiero przy próbie użycia tego
  keywordu, co bywa mylące, jeśli import wygląda poprawnie.
- Trzymanie zmiennych środowiskowych (URL, dane logowania) rozrzuconych po
  wielu plikach `.resource` zamiast w jednym miejscu - zmiana środowiska
  testowego wymaga wtedy edycji kilku plików zamiast jednego.
- Brak `Documentation` przy poszczególnych keywordach w resource'ach, przez
  co wygenerowany `libdoc` jest pusty/nieczytelny mimo poprawnej struktury
  plików.

## 5. Ulepszenia z AI
- Wklej strukturę katalogów projektu i poproś Claude/Copilot o zaproponowanie
  właściwej hierarchii importów resource'ów (co powinno importować co), tak
  aby uniknąć ryzyka cykli już na etapie projektowania.
- Poproś AI o wygenerowanie szkieletu pliku `__init__.robot` na podstawie
  opisu, jakie kroki mają się wykonać raz przed/po całym katalogu testów
  (np. logowanie do systemu, czyszczenie danych testowych).
- Wklej listę keywordów z kilku resource'ów i poproś AI o wykrycie kolizji
  nazw oraz propozycję bardziej jednoznacznego nazewnictwa.
- Użyj AI do wygenerowania dokumentacji Markdown/HTML na podstawie kodu
  resource'a, gdy brakuje czasu na ręczne uzupełnianie `Documentation` przy
  każdym keywordzie (a następnie zweryfikuj i popraw wygenerowany opis).
- Poproś AI o audyt pliku zmiennych pod kątem wartości, które powinny być
  parametryzowane per środowisko (dev/staging/prod), zamiast być zaszyte na
  sztywno w resource'ach.

## Zadanie na dziś
Rozbuduj strukturę z Dnia 10 (lub własny projekt) o: (1) drugi plik resource,
który importuje pierwszy (np. `checkout.resource` importujący
`common.resource`), (2) plik `tests/__init__.robot` z jednym wspólnym
`Suite Setup`/`Suite Teardown` dla całego katalogu, (3) wygeneruj dokumentację
jednego z resource'ów poleceniem `python -m robot.libdoc` i sprawdź w
przeglądarce, czy wszystkie keywordy mają czytelny opis.

================================================================================
# Dzień 29 (2026-08-27, czwartek) - Custom Keywords i własne biblioteki w Pythonie runda 2 (type hints, embedded arguments z regex, tagi keywordów, logger, integracja z BuiltIn)

## 1. Temat dnia
Druga runda tematu Custom Keywords - tym razem techniki pomijane przy
pierwszym podejściu: automatyczna konwersja argumentów przez type hints,
embedded arguments z własnym wzorcem regex w Pythonie, nadawanie tagów
keywordom bezpośrednio w kodzie, poprawne logowanie przez `robot.api.logger`
zamiast `print()` oraz wywoływanie innych keywordów RF z poziomu metody
Pythona przez `BuiltIn().run_keyword()`.

## 2. Kluczowe zasady i najlepsze praktyki
- Dodawaj type hints do sygnatury metody (np. `def dodaj(self, a: int, b:
  int) -> int`) - Robot Framework automatycznie konwertuje przekazane
  argumenty tekstowe na właściwy typ przed wywołaniem, więc nie trzeba
  ręcznie wołać `int()`/`float()` wewnątrz keyworda.
- Embedded arguments w Pythonie definiuje się tym samym `@keyword(...)`, ale
  można ograniczyć dopasowanie własnym regexem, np. `@keyword("Ustaw status
  zamowienia ${numer:\\d+} na ${status}")`, żeby uniknąć przypadkowego
  dopasowania do innego keyworda o podobnej nazwie.
- Tagi można nadać keywordowi już w kodzie przez `@keyword(tags=["api",
  "smoke"])` - każdy test korzystający z tego keyworda automatycznie
  dziedziczy te tagi, bez potrzeby powtarzania ich w `[Tags]` w każdym
  teście.
- Do logowania z poziomu biblioteki Pythona używaj `robot.api.logger`
  (`logger.info`, `logger.warn`, `logger.debug`), nigdy `print()` -
  `print()` trafia do konsoli, a nie do `log.html`, więc informacja ginie
  przy standardowym przebiegu CI.
- Gdy metoda Pythona musi wykonać inny keyword RF (np. z SeleniumLibrary
  albo z resource'u), użyj `BuiltIn().run_keyword("Nazwa Keyworda", arg1,
  arg2)` zamiast duplikować logikę - to jedyny poprawny sposób łączenia
  kodu Pythonowego z warstwą keywordów RF.

## 3. Przykład kodu
Biblioteka Pythona `libraries/OrderLibrary.py` z type hints, embedded
argumentem regex, tagami i logerem:
```python
from robot.api import logger
from robot.api.deco import keyword, library
from robot.libraries.BuiltIn import BuiltIn


@library(scope="TEST")
class OrderLibrary:
    """Biblioteka RF pokazujaca zaawansowane techniki custom keywordow."""

    def __init__(self):
        self._zamowienia = {}

    @keyword("Utworz Zamowienie O Wartosci ${wartosc:\\d+}", tags=["order", "api"])
    def utworz_zamowienie(self, wartosc: int):
        numer = len(self._zamowienia) + 1
        self._zamowienia[numer] = wartosc
        logger.info(f"Utworzono zamowienie nr {numer} o wartosci {wartosc}")
        return numer

    @keyword("Suma Zamowien Powinna Byc Rowna", tags=["order", "assertion"])
    def suma_zamowien_powinna_byc_rowna(self, oczekiwana: int):
        suma = sum(self._zamowienia.values())
        if suma != oczekiwana:
            raise AssertionError(f"Oczekiwano sumy {oczekiwana}, otrzymano {suma}")

    @keyword("Zaloguj I Utworz Zamowienie")
    def zaloguj_i_utworz_zamowienie(self, login: str, wartosc: int):
        BuiltIn().run_keyword("Log", f"Logowanie jako {login}")
        return self.utworz_zamowienie(wartosc)
```

Użycie w pliku testowym `tests/orders.robot`:
```robotframework
*** Settings ***
Documentation     Test biblioteki OrderLibrary - runda 2 custom keywords.
Library           ${CURDIR}/../libraries/OrderLibrary.py

*** Test Cases ***
Utworzenie dwoch zamowien i weryfikacja sumy
    [Tags]    order-flow
    Utworz Zamowienie O Wartosci 100
    Utworz Zamowienie O Wartosci 250
    Suma Zamowien Powinna Byc Rowna    350
```

## 4. Najczęstsze błędy
- Brak type hints przy argumentach numerycznych - RF przekazuje wszystko
  jako string, więc `a + b` konkatenuje teksty zamiast dodawać liczby, a
  błąd ujawnia się dopiero w asercji, nie przy wywołaniu keyworda.
- Użycie `print()` zamiast `robot.api.logger` - wiadomość znika w CI, gdzie
  nikt nie czyta konsoli, a `log.html` pozostaje bez żadnego śladu.
- Zbyt ogólny wzorzec regex w embedded argumentach (np. `${cokolwiek}` bez
  ograniczenia typu) - keyword zaczyna dopasowywać się do fragmentów
  zdań, które miały trafić do zupełnie innego keyworda.
- Tworzenie nowej instancji `BuiltIn()` w każdej linijce zamiast raz w
  metodzie - działa, ale utrudnia czytelność; lepiej przypisać do lokalnej
  zmiennej na początku metody, jeśli wywołań jest kilka.
- Nadpisanie nazwy istniejącego, wbudowanego keyworda (np. własny "Log")
  bez świadomości konfliktu - RF zgłasza niejednoznaczność albo po cichu
  używa złej implementacji, zależnie od `Library Search Order`.

## 5. Ulepszenia z AI
- Wklej istniejącą metodę biblioteki bez type hints i poproś Claude/Copilot
  o dodanie właściwych adnotacji typów na podstawie sposobu użycia
  argumentów w ciele metody.
- Poproś AI o zaproponowanie wzorca regex dla embedded argument, który nie
  będzie kolidował z innymi keywordami w tej samej bibliotece (podaj mu
  pełną listę istniejących nazw keywordów jako kontekst).
- Użyj AI do audytu biblioteki pod kątem `print()` użytego zamiast
  `robot.api.logger` - poproś o listę wszystkich wystąpień i gotową
  poprawkę.
- Poproś o wygenerowanie przykładu integracji z `BuiltIn().run_keyword()`
  dla konkretnego scenariusza (np. "wywołaj keyword z SeleniumLibrary z
  poziomu mojej metody Pythona").
- Wykorzystaj AI do zaproponowania zestawu tagów (`tags=[...]`) dla
  wszystkich keywordów w bibliotece na podstawie ich nazw i docstringów,
  tak aby selektywne uruchamianie testów było spójne w całym projekcie.

## Zadanie na dziś
Weź bibliotekę Pythona z Dnia 11 (lub własną) i rozbuduj ją o: (1) type
hints przy co najmniej jednym argumencie numerycznym, (2) jeden keyword z
embedded argumentem ograniczonym regexem (np. `${liczba:\\d+}`), (3) tagi
nadane bezpośrednio w `@keyword(tags=[...])`, (4) zamianę wszystkich
`print()` na `robot.api.logger`. Uruchom test i sprawdź w `log.html`, czy
komunikaty z logera są widoczne.

================================================================================
# Dzień 30 (2026-08-28, piątek) - Raportowanie i logi runda 2 (Log html=True, zrzuty ekranu w logu, redukcja rozmiaru logów, xunit.xml, Listener API)

## 1. Temat dnia
Druga runda tematu raportowania - techniki pomijane przy pierwszym podejściu:
osadzanie własnego HTML-a (w tym obrazków) bezpośrednio w log.html przez
`Log ... html=True`, dynamiczna zmiana poziomu logowania w trakcie testu
(`Set Log Level`), zmniejszanie rozmiaru dużych logów przez
`--removekeywords`/`--flattenkeywords`, generowanie `xunit.xml` do integracji
z Jenkins/GitHub Actions oraz podstawy Listener API do programowego
przechwytywania wyników.

## 2. Kluczowe zasady i najlepsze praktyki
- `Log    <b>tekst</b>    html=True` pozwala osadzić własny HTML (tabele,
  linki, pogrubienia) bezpośrednio w log.html - przydatne np. do wklejenia
  fragmentu odpowiedzi API w czytelnej tabeli zamiast surowego JSON-a.
- Zrzut ekranu zapisany na dysk (np. przez SeleniumLibrary/Browser) można
  osadzić w logu jako obrazek przez `Log    <img src="${plik}">    html=True`
  - dzięki temu screenshot jest widoczny bez opuszczania log.html.
- W dużych suite'ach (setki keywordów) używaj `--removekeywords PASS` albo
  `--flattenkeywords FOR`, żeby usunąć/spłaszczyć zbędne szczegóły w logu i
  znacząco zmniejszyć rozmiar pliku, nie tracąc informacji o testach FAIL.
- Generuj `--xunit output.xml` przy uruchamianiu `robot`, aby wtyczki CI
  (Jenkins JUnit, GitHub Actions test-reporter) mogły pokazać wyniki testów
  natywnie w interfejsie builda, a nie tylko jako link do artefaktu.
- Listener API (`--listener MyListener.py`) pozwala podpiąć własny kod pod
  zdarzenia RF (start/end testu, keyworda, wiadomość logu) - stosuj go do
  wysyłania powiadomień w czasie rzeczywistym (np. na Slacka) albo do
  zapisu własnych metryk, zamiast parsować output.xml po fakcie.

## 3. Przykład kodu
```robotframework
*** Settings ***
Documentation     Zaawansowane raportowanie - HTML w logu i dynamiczny loglevel.
Library           OperatingSystem

*** Test Cases ***
Osadzenie tabeli HTML w log.html
    [Documentation]    Pokazuje wynik "API" w czytelnej tabeli zamiast JSON-a.
    [Tags]    reporting
    ${tabela}=    Catenate
    ...    <table border="1"><tr><th>Pole</th><th>Wartosc</th></tr>
    ...    <tr><td>status</td><td>200</td></tr>
    ...    <tr><td>uzytkownik</td><td>jan.kowalski</td></tr></table>
    Log    ${tabela}    html=True

Dynamiczna zmiana poziomu logowania w trakcie testu
    [Documentation]    Wlacza DEBUG tylko na czas krytycznego fragmentu.
    [Tags]    reporting
    Log    Ten wpis widoczny przy standardowym INFO
    ${poprzedni}=    Set Log Level    DEBUG
    Log    Ten wpis widoczny tylko przy --loglevel DEBUG lub nizej    level=DEBUG
    Set Log Level    ${poprzedni}
    Log    Powrot do poprzedniego poziomu logowania
```

Minimalny Listener API do logowania każdego FAIL-a do osobnego pliku
(`listeners/FailListener.py`):
```python
class FailListener:
    ROBOT_LISTENER_API_VERSION = 3

    def __init__(self):
        self.plik = open("fail_report.txt", "a")

    def end_test(self, data, result):
        if not result.passed:
            self.plik.write(f"FAIL: {data.name} - {result.message}\n")

    def close(self):
        self.plik.close()
```

Przydatne polecenia CLI:
```
robot --xunit xunit.xml --outputdir results tests/
robot --removekeywords PASS --flattenkeywords FOR --outputdir results tests/
robot --listener listeners/FailListener.py tests/
rebot --removekeywords WUKS --output raport_skrocony.xml results/output.xml
```

## 4. Najczęstsze błędy
- Osadzanie dużych/wielu obrazków przez `html=True` w każdym kroku testu -
  log.html potrafi urosnąć do setek MB i przestaje się otwierać w
  przeglądarce; embeduj zrzuty tylko przy FAIL albo w kluczowych punktach.
- Wpisywanie niezaufanego/niezescapowanego tekstu (np. odpowiedzi z API) z
  `html=True` - jeśli treść zawiera znaki `<`/`>`, mogą zepsuć strukturę
  logu albo (przy danych z zewnątrz) wprowadzić przypadkowy HTML injection
  we własnym raporcie.
- Zostawienie `Set Log Level    DEBUG` bez przywrócenia poprzedniego
  poziomu - reszta testu (i kolejne testy w tym samym wątku) logują dużo
  więcej niż powinny, zaśmiecając log.html.
- Brak `--xunit` w pipeline CI, mimo że wtyczka do raportowania testów tego
  wymaga - build pokazuje tylko "success/failure", bez rozbicia na
  poszczególne testy w interfejsie Jenkinsa/GitHub Actions.
- Pisanie własnego Listenera, który rzuca wyjątek w metodzie `end_test`/
  `end_keyword` - błąd w listenerze potrafi przerwać cały przebieg testów,
  dlatego kod listenera powinien mieć własną obsługę wyjątków.

## 5. Ulepszenia z AI
- Wklej fragment surowej odpowiedzi JSON z API i poproś Claude/ChatGPT o
  wygenerowanie gotowego kodu `Catenate` budującego czytelną tabelę HTML do
  `Log ... html=True`.
- Poproś AI o wygenerowanie szkieletu prostego Listenera Pythonowego pod
  konkretny cel (np. wysyłka powiadomienia na Slacka przy FAIL, zapis
  metryk czasu trwania testów do CSV).
- Użyj AI do zaproponowania, w których miejscach dużego suite'u warto użyć
  `--removekeywords`/`--flattenkeywords`, na podstawie opisu, które
  fragmenty logu są najmniej istotne (np. pętle FOR z wieloma iteracjami).
- Wklej fragment `xunit.xml` lub konfigurację joba CI i poproś AI o
  weryfikację, czy wtyczka raportująca (JUnit plugin, test-reporter) jest
  poprawnie podpięta pod wygenerowany plik.
- Poproś AI o przegląd własnego kodu Listenera pod kątem brakującej obsługi
  wyjątków, która mogłaby przerwać cały przebieg testów w razie błędu.

## Zadanie na dziś
Weź dowolny istniejący test i: (1) dodaj krok logujący dane w formie tabeli
HTML przez `Log ... html=True`, (2) otocz jeden fragment testu
`Set Log Level    DEBUG` / przywróceniem poprzedniego poziomu, (3) uruchom
suite z flagą `--xunit xunit.xml` i sprawdź wygenerowany plik, (4) napisz
minimalny Listener logujący nazwę każdego testu do konsoli przez
`start_test` i podepnij go flagą `--listener`.

================================================================================
# Dzień 31 (2026-08-29, sobota) - Obsługa wyjątków runda 2 (Continue On Failure, wiele wzorców w EXCEPT, własne klasy wyjątków, Run Keyword And Return Status)

## 1. Temat dnia
Druga runda tematu obsługi wyjątków - techniki pomijane przy pierwszym
podejściu: `Run Keyword And Continue On Failure` do zbierania wielu błędów
w jednym przebiegu, dopasowywanie kilku wzorców błędów w jednej klauzuli
`EXCEPT`, rzucanie i łapanie własnych klas wyjątków zdefiniowanych w
bibliotece Pythonowej oraz `Run Keyword And Return Status` do warunkowego
sterowania przepływem testu.

## 2. Kluczowe zasady i najlepsze praktyki
- `Run Keyword And Continue On Failure` oznacza krok jako niepowodzenie, ale
  NIE przerywa testu - przydatne przy zbiorczej weryfikacji kilku
  niezależnych asercji naraz i zebraniu wszystkich błędów w jednym raporcie
  zamiast przerywania na pierwszym z nich.
- Jedna klauzula `EXCEPT` może łapać kilka wzorców na raz, np.
  `EXCEPT    ValueError: *    TypeError: *` - rozdzielone spacją wzorce są
  traktowane jako alternatywa (dowolny z nich pasuje).
- Definiuj własne klasy wyjątków w bibliotece Pythona (`class
  ZamowienieError(Exception)`) zamiast rzucać generyczny `Exception` -
  pozwala to precyzyjnie łapać konkretny typ błędu biznesowego w `EXCEPT`
  i odróżnić go od błędów programistycznych.
- `Run Keyword And Return Status` zwraca `${True}`/`${False}` zamiast
  rzucać/zgłaszać błąd - dobre do sterowania logiką testu (np. IF na
  podstawie tego, czy krok się powiódł), ale NIE zastępuje właściwej
  asercji, bo niepowodzenie nie trafia do logu jako FAIL.
- Nie zagnieżdżaj TRY/EXCEPT zbyt głęboko w plikach .robot - jeśli logika
  obsługi błędów robi się rozbudowana, przenieś ją do keyworda/biblioteki
  Pythona, gdzie łatwiej ją przetestować i utrzymać.

## 3. Przykładowy kod
Biblioteka Pythona z własną klasą wyjątku `libraries/OrderValidator.py`:
```python
from robot.api import logger


class ZamowienieError(Exception):
    """Wlasny wyjatek dla bledow logiki biznesowej zamowien."""


class OrderValidator:
    def waliduj_zamowienie(self, wartosc: int):
        if wartosc <= 0:
            raise ZamowienieError(f"Nieprawidlowa wartosc zamowienia: {wartosc}")
        logger.info(f"Zamowienie o wartosci {wartosc} jest poprawne")
        return True
```

Plik testowy `tests/exceptions_runda2.robot`:
```robotframework
*** Settings ***
Documentation     Runda 2: obsluga wyjatkow - Continue On Failure, wiele wzorcow, wlasne wyjatki.
Library           ${CURDIR}/../libraries/OrderValidator.py

*** Test Cases ***
Zbiorcza weryfikacja kilku warunkow bez przerywania testu
    [Documentation]    Continue On Failure zbiera wszystkie bledy w jednym przebiegu.
    [Tags]    exceptions-runda2
    Run Keyword And Continue On Failure    Should Be Equal    ${1}    ${2}
    Run Keyword And Continue On Failure    Should Contain    abc    z
    Log    Test dotarl do konca mimo dwoch niepowodzen powyzej

Lapanie kilku typow bledow jedna klauzula EXCEPT
    [Documentation]    Jedna klauzula EXCEPT z dwoma wzorcami wyjatkow.
    [Tags]    exceptions-runda2
    FOR    ${wejscie}    IN    abc    ${None}
        TRY
            Evaluate    int('${wejscie}')
        EXCEPT    ValueError: *    TypeError: *    AS    ${blad}
            Log    Zlapano oczekiwany blad konwersji: ${blad}
        END
    END

Lapanie wlasnego wyjatku po typie
    [Documentation]    EXCEPT dopasowuje sie po nazwie wlasnej klasy wyjatku.
    [Tags]    exceptions-runda2
    TRY
        OrderValidator.Waliduj Zamowienie    -50
    EXCEPT    ZamowienieError: *    AS    ${blad}
        Log    Zlapano wlasny wyjatek biznesowy: ${blad}
    END

Warunkowy przeplyw z Run Keyword And Return Status
    [Documentation]    Sprawdzenie statusu bez traktowania bledu jako FAIL.
    [Tags]    exceptions-runda2
    ${czy_poprawne}=    Run Keyword And Return Status    OrderValidator.Waliduj Zamowienie    100
    IF    ${czy_poprawne}
        Log    Zamowienie przeszlo walidacje
    ELSE
        Log    Zamowienie odrzucone przez walidacje    WARN
    END
```

## 4. Najczęstsze błędy
- Nadużywanie `Run Keyword And Continue On Failure` do każdego kroku testu -
  raport staje się nieczytelny (dużo failures naraz) i trudno rozpoznać,
  które niepowodzenie było przyczyną, a które tylko skutkiem.
- Użycie `Run Keyword And Return Status` tam, gdzie potrzebna jest właściwa
  asercja - błąd biznesowy zamienia się w ciche `${False}`, więc test może
  zakończyć się PASS mimo realnego problemu, jeśli ktoś zapomni sprawdzić
  zwróconą wartość.
- Łapanie zbyt szerokiego typu (`Exception: *` zamiast konkretnej klasy) w
  `EXCEPT` - test przechodzi nawet wtedy, gdy rzucony zostanie zupełnie
  inny, nieoczekiwany błąd niż ten faktycznie testowany.
- Rzucanie generycznego `Exception`/`AssertionError` zamiast własnych klas
  wyjątków w bibliotece Pythona - w testach nie da się odróżnić błędu
  walidacji biznesowej od błędu programistycznego (np. literówki w kodzie).
- Głębokie zagnieżdżanie TRY/EXCEPT bezpośrednio w plikach .robot zamiast
  wydzielenia logiki do keyworda/biblioteki Pythona - kod staje się trudny
  do śledzenia i utrzymania przez cały zespół.

## 5. Ulepszenia z AI
- Wklej istniejący plik testowy z wieloma pojedynczymi asercjami i poproś
  Claude/Copilot o wskazanie, które z nich warto połączyć przez `Run
  Keyword And Continue On Failure`, by zebrać wszystkie błędy w jednym
  przebiegu.
- Poproś AI o zaprojektowanie hierarchii własnych klas wyjątków (np.
  `ZamowienieError`, `PlatnoscError` dziedziczące po wspólnej bazie) na
  podstawie opisu logiki biznesowej aplikacji.
- Wklej fragment kodu Pythona rzucającego generyczny `Exception` i poproś o
  refaktoryzację na konkretne, nazwane klasy wyjątków wraz z odpowiadającymi
  wzorcami `EXCEPT` w Robot Framework.
- Użyj AI do przeglądu testów pod kątem nadużycia `Run Keyword And Return
  Status` w miejscach, gdzie w rzeczywistości potrzebna jest jawna
  asercja/FAIL.
- Poproś o wygenerowanie zestawu przypadków testowych dla różnych kombinacji
  błędów (np. tabela wejście → oczekiwany typ wyjątku) na podstawie opisu
  walidacji API.

## Zadanie na dziś
Rozbuduj bibliotekę Pythona o własną klasę wyjątku (np. `PlatnoscError`)
rzucaną przy niepoprawnej kwocie płatności, a następnie napisz: (1) test z
blokiem TRY/EXCEPT łapiącym ten konkretny typ wyjątku, (2) test używający
`Run Keyword And Continue On Failure` do zebrania kilku niezależnych
asercji w jednym przebiegu, (3) test wykorzystujący `Run Keyword And Return
Status` do warunkowego sterowania logiką (IF/ELSE) w zależności od wyniku
walidacji.

================================================================================
